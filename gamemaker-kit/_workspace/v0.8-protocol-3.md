# v0.8 Protocol 3 — Pre-release evaluator report

**Date**: 2026-05-18 (00:15 KST)
**Anchor**: HANDOFF.md v0.8 backlog (lines 27-90), Protocol 1 report `_workspace/v0.8-protocol-1.md`.
**Working-tree HEAD**: `e084c78` (v0.7.0). All v0.8 work uncommitted.
**Pre-flight**: `bash scripts/check-plugin-meta.sh` exits 0 with all 7 checks PASS, 0 warnings.

---

## 1. Verdict

**ACCURATE.**

All 6 Tier-1 + Tier-2 candidates claimed by the v0.8 author are verified present, correctly placed, and behave as advertised. No new defects introduced. The pre-flight is clean for the first time at this depth (7 checks, 0 warnings — Check G fires correctly: "every pillar in the example declares a valid kind").

A few small things sit just below the bar for "Major" — itemized in §3 as Minor. None of them blocks the v0.8.0 tag.

---

## 2. Per-candidate verification

| Candidate | Verdict | Evidence |
|---|---|---|
| T1-A (M-1 + endpoint precision/promote) | **PASS** | See §2.1 |
| T1-B (Rule 17 + Check G + read-site migration) | **PASS** | See §2.2 |
| T1-C (HANDOFF template + Protocol-1 retro) | **PASS** | See §2.3 |
| T2-A (Mock cleanup reminder from gmk-art-gen) | **PASS** | See §2.4 |
| T2-B (Shader 401→299 + scope justification) | **PASS** | See §2.5 |
| M-1 (Allowlist staleness audit) | **PASS** | See §2.6 (also §2.1 — co-implemented with T1-A) |

**6 PASS / 0 FAIL / 0 PARTIAL.**

### 2.1 T1-A — endpoint Check B precision + WARN→FAIL promote

- Regex tightened: `scripts/check-plugin-meta.sh:173` uses `grep -rnIwE 'endpoint'` (whole-word via `-w`, no `-i`). Functionally equivalent to `\bendpoint\b`. **PASS**.
- Severity promoted: `scripts/check-plugin-meta.sh:156` header comment now says *"FAIL-level in v0.8"*. In execution path the `endpoint_hits` accumulator feeds `drift`, not `warn` (line 180) — so a future drift would block release (exit 1). **PASS**.
- Stale entry removed: `scripts/.endpoint-allowlist.txt` no longer contains `marketplace.json:8`. The 6 remaining entries are the 6 currently-detected live occurrences (verified by re-running the Check B regex — all 6 are allowlisted, `endpoint_hits=0`). **PASS**.
- Allowlist policy documented: `.endpoint-allowlist.txt:19-23` declares *"Check B uses `grep -wE 'endpoint'` (whole-word, no -i)"* and explains why `-i` was dropped. **PASS**.

### 2.2 T1-B — Rule 17 + Check G + read-site migration

- Rule 17 spec at `skills/gmk-prototype-rules/SKILL.md:485-521`, structured exactly parallel to Rule 16:
  - 4-case table at L491-496: valid enum / absent (fall back to free-text) / unknown enum (warn + fall back) / wrong casing (warn + normalize). Mirrors Rule 16's 4-case table form (`SKILL.md:458-463`). **PASS**.
  - Enum list at L498: `sensory / behavioral / decision-shape / emotional` lowercase, with explicit note that the hyphen in `decision-shape` is significant.
  - "Why warn-only" rationale at L513-515 matches Rule 16's parallel "Why warn-only, not refuse" section. **PASS**.
- Check G added at `scripts/check-plugin-meta.sh:291-328`:
  - Counts pillars in `_workspace/examples/pillars-example.json` via `grep -cE '^\s*"id":'`; counts valid kinds via `grep -cE '^\s*"kind":\s*"(sensory|behavioral|decision-shape|emotional)"'`. Mental test: a pillar with `"kind": "Sensory"` (wrong casing) or `"kind": "garbage"` (bad enum) would fail the second grep, `kind_valid < pillar_count`, the `bad_kinds` enumeration would surface the offending line. Catches all 4 contract cases. **PASS**.
  - WARN level (not FAIL — author's claim is accurate: `warn=$((warn + 1))` at line 324, not `drift`).
- Read-site migrations:
  - `skills/gmk-prototype/SKILL.md:78` explicitly states the algorithm: *"Read the pillar's `kind` field first (per gmk-prototype-rules Rule 17 — values: sensory / behavioral / decision-shape / emotional). If `kind` is absent (pre-v0.4 pillars or hand-edited files), fall back to free-text classification on the pillar's `name` + `description`."* Bullets at L80-83 show each kind value in code (`kind: "behavioral"` etc.). **PASS** — this is the prose-spec version of the algorithm, which is the canonical form for SKILL.md content (SKILL.md is read by the model, not executed).
  - `skills/gmk-self-test/SKILL.md:303` mirrors the same algorithm: *"Read the pillar's `kind` field first (per gmk-prototype-rules Rule 17). If `kind` is absent, fall back to free-text classification ..."*. The routing row at L307 is updated: *"the pillar's `kind` is `\"sensory\"` (or, on `kind` absent, free-text matches ...)"* — field-first, free-text fallback explicit. **PASS**.
- Writer-side update at `skills/gmk-init/SKILL.md`:
  - Schema example at L172 now has `"kind": "sensory"` on the example pillar. **PASS**.
  - L188 has a new paragraph: *"`kind` is the pillar shape the user picked in Step 1/2 (one of `sensory` / `behavioral` / `decision-shape` / `emotional`). Write it on every pillar — downstream skills (`gmk-prototype`, `gmk-self-test`) read it per `gmk-prototype-rules` Rule 17 ..."* Also covers hybrid pillars + the legacy free-text fallback. **PASS**.
- Footer amendment:
  - Exactly 2 SKILLs amended: `gmk-prototype/SKILL.md:26` and `gmk-self-test/SKILL.md:42` both say *"Rule 13-14, 16, 17"* with the new precondition phrase *"pillars.kind read contract"*. The other 25 SKILL footers still say *"Rule 13-14, 16"* (verified by grep — see search results). This matches the design intent (only shape-routing SKILLs need Rule 17). **PASS**.
  - Rule 17 spec at `gmk-prototype-rules/SKILL.md:511` self-documents this: *"The Rule 13-14 footer is amended in v0.8 to Rule 13-14, 16, 17 in SKILLs that read pillar shape."* No contradiction.
- pillars-example.json (`_workspace/examples/pillars-example.json`): all 3 pillars have `kind` (`sensory`, `decision-shape`, `behavioral`) at lines 11/22/33. Check G says *"pillars in example: 3, valid kind declarations: 3"*. **PASS**.

### 2.3 T1-C — HANDOFF backlog template + Protocol-1 retro

- `_workspace/handoff-backlog-template.md` exists (115 lines). Structure:
  - 4 required fields: (1) file:line targets + grep query, (2) non-targets with reasons, (3) classification tag (sweep/single-fix/structural-guard/process), (4) defect-class link. **PASS**.
  - F21 closure criterion at L91-93: "correction count < 1 per candidate for 3 consecutive releases". Matches the author's claim. **PASS**.
  - Example candidate at L48-70 uses T1-B itself as a worked example (good shape demonstration).
  - Tracking table at L97-101 with v0.6/v0.7/v0.8 ratios (v0.7=1.75, v0.8=2.80+3). The v0.8 row matches HANDOFF.md L93 numbers.
- HANDOFF.md v0.8 backlog references the template at L34: *"v0.8+부터 각 candidate는 `_workspace/handoff-backlog-template.md`의 shape를 따른다"*. **PASS**.
- Note on constraint enforcement: the template **constrains by convention**, not by mechanical check. The next HANDOFF author could ignore it; the next Protocol 1 evaluator would then have to either tolerate the violation or call it out. This is intentional (process candidate, not a structural-guard) and the template's existence at least gives evaluators a documented anchor to cite. The author was honest about this in HANDOFF L34: *"본 v0.8 backlog는 부분 적용 (template 자체가 v0.8 deliverable라 retro-fit 불가) — v0.9부터 full apply"*. **PASS** (window-dressing risk acknowledged in the design itself).

### 2.4 T2-A — Mock cleanup reminder from gmk-art-gen

- `skills/gmk-art-gen/SKILL.md:150-153` adds a *"Mock cleanup (only if a mocked sibling exists)"* sub-bullet inside the Step 6 *"Next:"* output block:
  > *"Real assets landed. The mocked file at prototypes/m2-dragon-evo-mocked.html is now stale — delete it and re-run /gmk-validate on the original (or keep both temporarily to side-by-side compare)."*
  Conditional ("only if a mocked sibling exists"), informational only (no auto-delete). **PASS**.
- `skills/gmk-mock-inject/SKILL.md:284` cross-reference updated: *"Doesn't auto-delete the mocked file when real assets arrive. The user deletes; the kit doesn't track. (v0.8: `/gmk-art-gen` prints a one-line reminder in its Step 6 summary when a mocked sibling exists — the reminder is informational, not a destructive auto-cleanup.)"*. **PASS**.
- Side effect correctly handled: T2-A's insertion shifted the recovery-instruction line from 140 → 144 in gmk-art-gen. `scripts/.rule14-allowlist.txt:30-32` was updated from `gmk-art-gen/SKILL.md:140` to `:144` with a v0.8-shift comment. Pre-flight Check A still passes (line 144 contains "re-run `/gmk-art-gen m2-dragon-evo --assets dragon-rg-idle`" — verified by reading L144). **PASS**.

### 2.5 T2-B — Shader template 401 → 299 + scope justification

- `templates/prototype-shader.html` line count: **299 lines** (under 300 soft cap). `wc -l` confirms. Diff stats: +60 / −162 (net −102 from 401). **PASS**.
- Bot hook API surface preserved (verified via grep at templates/prototype-shader.html:271-294):
  - `window.__gmk_botHook__ = window.__gmk.makeHook({ ... })` binding present (L271).
  - All 5 canonical callbacks present: `reset(seed, rng)` L272, `isOver()` L279, `legalActions()` L280, `apply(action)` L281, `collectSummary()` L282.
  - Optional v0.2 callbacks `stateSignature()` L284 and `progressEstimate()` L288 preserved. `riskEstimate / noveltyScore` documented as intentionally omitted at L292.
  - `_gmkApiVersion: 1` declared in `templates/_bot_hook_lib.js` (referenced via Check F path) — Check F passes.
- Diff inspection confirms refactor was: multi-line callbacks → single-line callbacks, comment compaction, whitespace normalization. No GLSL/FRAG_SRC functional change (the diff to FRAG_SRC is comment text only). **PASS**.
- Check E now reports: *"OK — all templates under soft cap (300 lines)"*. The baseline WARN is resolved.
- Scope justification: HANDOFF.md L71 carries the one-liner *"Check E intentionally not extended to skills/ — SKILL files are reference docs not prototypes; the 300/600 numbers were calibrated for mechanic-prototype mental-model load (Rule 2 rationale at gmk-prototype-rules:58), not doc length."* **PASS**.

### 2.6 M-1 — Allowlist staleness audit

- `.endpoint-allowlist.txt`: every entry (5 final + 1 reused as marketplace.json:15 commentary) carries a `verified-at: e084c78 (2026-05-17)` stamp in its justification comment. Convention documented at lines 9-17 of the file. **PASS**.
- `.rule14-allowlist.txt`: all 9 entries carry `verified-at: e084c78 (2026-05-17)` stamps; the one shifted entry (`gmk-art-gen:144`) carries `verified-at: HEAD (2026-05-18, v0.8)` with a v0.8-shift note. Convention documented at lines 18-23. **PASS**.
- M-1's own machinery caught the T2-A line shift — exactly the staleness drift Protocol 1 predicted. This is positive evidence that the convention works. **PASS**.

---

## 3. New defects introduced

No Critical or Major defects.

**Minor (do not block release)**:

1. **`_workspace/examples/pillars-example.json:2` `_comment` does not yet mention Rule 17.** The comment still reads *"kit_version is read v0.7+ per gmk-prototype-rules Rule 16 (warn-only on future versions)"* — Rule 17 should be added as a parallel sentence describing the read side of the `kind` field. This is the example file teaching users the schema; if you're writing a pillars.json you'd naturally read this comment first. **Severity: Minor.** Optional one-line addition; not a v0.8 release blocker.

2. **`skills/gmk-init/SKILL.md:163` schema example still declares `"kit_version": "0.7.0"`.** This is the example output users see when running `/gmk-init` on a fresh project under v0.8. The same goes for L196 milestones.json example. Will need a bump when plugin.json moves to 0.8.0 (which the task instructions said is the next post-Protocol-3 task). **Severity: Minor / expected** — flagging only so it isn't missed during the release-bump pass. Pre-flight Check D will catch this automatically (MAJOR.MINOR drift) the moment plugin.json bumps to 0.8.

3. **The `gmk-init/SKILL.md:188` paragraph about `kind` semantics is good prose, but does not explicitly call out the *wrong-casing* behavior** (one of the 4 Rule 17 cases). It says "Use the lowercase exact form" but doesn't explain what the kit does if the user writes `"Sensory"`. The Rule 17 spec itself covers it (`gmk-prototype-rules:496`), so the user can chase the link. **Severity: Minor.**

4. **`scripts/check-plugin-meta.sh:306` uses `head -1` after `grep -c`**, which is harmless (grep -c emits a single number) but vestigial. Cosmetic. **Severity: Trivial.**

No regressions in any other live behavior. No new "endpoint" tokens introduced into live scan paths (verified by re-running Check B regex — clean).

---

## 4. Protocol 1 correction-items reconciliation

Protocol 1 (`_workspace/v0.8-protocol-1.md`) raised 14 corrections across 5 candidates + 3 missing (M-1/M-2/M-3). Task instructions explicitly defer M-2 and M-3 to v0.9. Of the remaining 12 corrections:

| Protocol 1 candidate | Item | Reflected in v0.8? |
|---|---|---|
| #1 ADD (1) stale `.endpoint-allowlist.txt:37` | Removed | YES — `marketplace.json:8` removed; only `:15` remains |
| #1 ADD (2) `-i` policy clarification | Documented | YES — `.endpoint-allowlist.txt:19-23` |
| #1 VERIFY order: allowlist → regex → promote | Sequence followed | YES — current state is clean (`endpoint_hits=0`), promote is in effect |
| #2 ADD scope-justification paragraph | One-liner in HANDOFF | YES — HANDOFF.md:71 |
| #3 ADD concrete target site list | Listed in HANDOFF + executed | YES — gmk-prototype:78-82 + gmk-self-test:305 + gmk-init:41/65 all migrated |
| #3 REMOVE "either/or" framing | Both Rule 17 AND read-migration done | YES — not framed as alternatives |
| #3 RECLASSIFY (is Rule 17 not just a check) | New Rule 17 added | YES — gmk-prototype-rules:485-521 |
| #3 VERIFY sister fields read | Already verified in Protocol 1 | NOOP — no v0.8 action required |
| #4 ADD trigger location | gmk-art-gen Step 6 | YES — skills/gmk-art-gen/SKILL.md:150-153 |
| #4 RECLASSIFY (not Step 8) | Renamed | YES — HANDOFF.md T2-A describes "from gmk-art-gen completion path" |
| #5 ADD concrete check description | Template file | YES — `_workspace/handoff-backlog-template.md` |
| #5 RECLASSIFY (process not check) | Documented | YES — HANDOFF.md:56-57 explicitly "(process, not check)" and *"`scripts/`에 안 들어감"* |
| M-1 (allowlist staleness) | Co-implemented with #1 | YES — both allowlist files now carry SHA stamps |

**12 of 12 actionable corrections reflected.** Protocol 1 #4 secondary trigger ("gmk-validate when called on `-mocked.html`") is NOT implemented in v0.8 — the author opted for the simpler single-trigger version. This is a defensible scope decision (HANDOFF T2-A:66 lists it as "secondary trigger 후보") and the gmk-art-gen trigger covers the primary case. Acceptable.

---

## 5. Release recommendation

**GREEN-LIGHT v0.8.0 tag.**

All claimed work is verified present and correct. Pre-flight passes 0/0/0 (drift/warn/fail). Bot hook API contract preserved through the shader refactor. No new defects introduced. Protocol 1 correction items all addressed. The 3 Minor items in §3 are post-tag polish or expected pre-bump state.

**Recommended next steps after this Protocol 3 PASS** (in order):

1. Bump `.claude-plugin/plugin.json` + `marketplace.json` to `0.8.0`. Pre-flight Check D will then flag the v0.7.0 in the schema examples — bump those too in the same commit.
2. Update CHANGELOG.md with v0.8 entry (include the Protocol 1 corrections retrospective field per the new template at `_workspace/handoff-backlog-template.md:78-89`).
3. Optional Minor #1 from §3 (one-line edit to `_workspace/examples/pillars-example.json:2` `_comment` adding Rule 17 mention) — can ship in v0.8 or wait for v0.9.
4. Commit, tag, push, then run Protocol 4 (post-release git verification).

---

## Appendix — Verification commands run

- `bash scripts/check-plugin-meta.sh` → exit 0, 7/7 PASS, 0 warnings.
- `grep -rnIwE 'endpoint' skills CONCEPT.md README.md _workspace/structure.md .claude-plugin/ | grep -vE '^_workspace/v0\.[0-9]+-'` → 6 occurrences, all allowlisted.
- `wc -l templates/prototype-*.html` → 299 / 243 / 236 / 259 (all under 300).
- `grep -nE 'Rule 13-14, 16, 17|Rule 13-14, 16' skills/**/SKILL.md` → 2 amended (`gmk-prototype`, `gmk-self-test`), 25 untouched at Rule 16. No accidental over-application.
- Diff stats for `templates/prototype-shader.html`: +60 / −162 lines, no API-surface change in the 5 canonical callbacks.
