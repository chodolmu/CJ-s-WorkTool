---
title: Codex adversarial review of S1 Step 3 (Stage 1+2) execution plan
date: 2026-05-29
session: Phase γ
status: applied_next_session
codex_session: a250694ad373e4121
---

# Codex Step 3 검증 — Verdict: SHIP-WITH-CHANGES

검증 시점: 2026-05-29 ~04:55 KST, Phase γ (S1 Step 0+1 PASS 직후, Step 3 실행 *전*).
검증 트리거: 사용자 요청 "해당 내용 코덱스로 검증해서 진행해봐".

## Codex 출력 verbatim

```
VERDICT: SHIP-WITH-CHANGES

**1. Stage 1 Query Design**
`"merge3"` is too brittle — the genre is inconsistently named across sources, and the `site:` restriction risks returning zero results before the fallback triggers. Fix: replace both prescriptions in `v1.0-detail-S1.md:142-147` with:

- Search A: `("merge-3" OR "merge 3" OR "merge puzzle" OR "merge game" OR "match-3 merge") (mechanics OR design OR progression OR economy) (site:gamedeveloper.com OR site:gdcvault.com OR site:gamasutra.com)`
- Search B: `("merge-3" OR "merge puzzle" OR "merge game" OR "match-3 merge") (postmortem OR retrospective OR "game design" OR interview)`

**2. Royal Match SEO Contamination**
Stage 4 is too late. Stage 2's current gate (`v1.0-detail-S1.md:148-153`) only rejects bare store pages, so all three shortlisted references can be SEO-grade before extraction burns budget. Fix: add a dev-grade eligibility rule at Stage 2 — `source_type must be dev-blog | postmortem | academic | gdc | interview`; listicle/marketing sources do not count toward `len(references) >= 3`.

**3. Reference Count >= 3 + Genre Disambiguation**
The seed has a genre/reference mismatch: `genre: "merge3"` but `reference_titles: ["Royal Match"]` — Royal Match is match-3-with-meta, not pure-merge. Also the seed file has an unterminated JSON string, which triggers the Step 1 JSON.parse quit signal (`v1.0-detail-S1.md:103-110`). Fix: (a) do not run Stage 1 until `seed.json` parses cleanly, and (b) add a mandatory `target_family: "match3-with-meta" | "pure-merge" | "hybrid"` field before Stage 1 — ask user only if missing or inconsistent with the seeded reference.

**4. Cost Discipline**
Stage 1+2 looks safe, but the real trap is Stage 3: worst-case `90 search + 90 fetch` against a 100-call hard cap (`v1.0-detail-S1.md:392`). If Stage 2 selects 5 references, Stage 3 is budget-blown before cross-verification finishes. Fix: hard-cap Stage 2 to exactly 3 references for S1 dogfood; reserve at least 40 calls for Cycle 3.

**5. Research-Distilled Applicability**
No Gap applies directly to Stage 1+2 execution, but Gap 3's adversarial review pattern points to a structural omission: there is no pre-Stage-1 "query + source eligibility review" artifact. Fix: add Step 0.75 that writes approved queries, disambiguation result, and Stage 2 source eligibility rules to `research-notes.md` before any web calls.

---

Concrete fix list:
- `seed.json` — fix unterminated JSON string (blocks Step 1 quit gate)
- `v1.0-detail-S1.md:142-147` — replace genre query strings with multi-term alternatives above
- `v1.0-detail-S1.md:148-153` — add dev-grade source eligibility rule to Stage 2 shortlist criteria
- `v1.0-detail-S1.md` Stage 2 — add `target_family` disambiguation field; hard-cap shortlist to 3 for dogfood
- `v1.0-detail-S1.md` §2 — add Step 0.75 pre-flight artifact before Stage 1
```

## Claude 해석 (Codex 외부 시각 → 우리 컨텍스트 매핑)

| # | Codex 지적 | 진짜 신호? | 다음 세션 액션 |
|---|-----------|----------|--------------|
| 1 | Stage 1 query brittle (`"merge3"` 단일 키워드 + 좁은 site: 제한) | **YES** | detail-S1.md §2 Step 3 Stage 1 query 2개를 Codex 제안 multi-term + 확장된 site: alternation으로 교체 |
| 2 | Stage 4가 너무 늦음, Stage 2가 dev-grade 필터 시작해야 | **YES** | detail-S1.md §2 Step 3 Stage 2 quit signal에 `source_type ∈ {dev-blog, postmortem, academic, gdc, interview}` 추가. listicle/marketing은 `references[]` 카운트 미포함. |
| 3a | seed.json JSON 파싱 깨짐 | **NO (false positive)** | 실제 `node -e require('./seed.json')` 정상. Codex가 표시 잘림을 손상으로 오인. 무시. |
| 3b | merge3 ↔ Royal Match family 불일치 (Royal Match는 match-3-with-meta, pure-merge 아님) | **YES** | seed.json에 `target_family: "match3-with-meta"` 필드 추가 (사용자 결정 받음 — Royal Match + Gardenscapes 패밀리). detail-S1.md Stage 2 절차에 family 필드 반영 step 박기. |
| 4 | Stage 3 cost 폭주 (worst-case 90+90 vs 100 cap) | **YES** | detail-S1.md §2 Step 3 Stage 2 quit signal "references >= 3" → **"references == 3 (hard cap, no 4-5)"**으로 strict 변경. §6 cost cap 산식 재계산: 3 refs × 3 cycles × 6 categories = ~54+54, Cycle 3 cross-verify ~30+ calls 여유 확보. |
| 5 | Stage 1 전 pre-flight artifact 누락 (Gap 3 adversarial pattern 구조 적용) | **YES (구조적)** | detail-S1.md §2에 새 sub-step "Step 3.0 — Pre-flight" 추가: 승인된 query + target_family + Stage 2 source-type rule을 `research-notes.md` 머리에 박은 *후에만* Stage 1 web call 시작. |

## 사용자 결정 (2026-05-29 04:55 KST)

| # | 질문 | 답 |
|---|------|---|
| Q1 | Codex 4 fix 어떻게 반영? | **일단 handoff 저장, 다음 세션에서 적용 + Step 3 진입** |
| Q2 | target_family | **match3-with-meta (Royal Match, Gardenscapes)** |

## 다음 세션 진입 시 정확한 액션 sequence

1. **4-file read protocol** 그대로 (concept + resume + research-distilled + detail-S1)
2. **이 파일 (`v1.0-codex-S1-step3-review.md`)도 정독** — 5번째 파일로 흡수
3. **Step 0 Resume Verification** 재실행 (P8, 새 세션 disk 검증)
4. **detail-S1.md 편집** (4 fix + 1 새 sub-step):
   - Step 3 Stage 1 query 2개 multi-term으로 교체
   - Step 3 Stage 2 quit signal에 dev-grade source-type 필터 추가
   - Step 3 Stage 2 reference count `>= 3` → `== 3` (hard cap)
   - 새 Step 3.0 (Pre-flight) sub-step 추가
   - §6 cost cap 산식 3-refs-만으로 재계산
5. **seed.json 편집** — `target_family: "match3-with-meta"` 필드 추가, `_meta`에 family 결정 출처 (이 review file cite)
6. **detail-S1.md 변경 후 evaluator 1회 검토** (정합성 회귀 없는지 확인)
7. **Step 3 실행** — 수정된 query로 Stage 1+2

## 검증 비용
- 1 codex:rescue invocation, foreground, 약 110초
- 본 review 작성 비용 = 0 web call
- Codex agent id = `a250694ad373e4121` (필요 시 SendMessage로 후속 가능)

## 파일 인덱스 위치
- `_workspace/v1.0-detail-S1.md` — fix 대상 (다음 세션 편집)
- `_workspace/s1-test/seed.json` — target_family 추가 대상 (다음 세션 편집)
- `_workspace/v1.0-resume.md` — Phase γ 상태 + 다음 액션 명시 (이 세션 atomic write)
- `HANDOFF.md` — Phase γ 섹션 추가 (이 세션)
- 이 파일 — Codex 검증 frozen record (이 세션 commit)
