# v0.8 Protocol 4 — Post-release evaluator report

**Date**: 2026-05-18 (00:30 KST)
**HEAD**: `b0a59c0c7451bacc627250e29049f9033619ffc7` (= annotated tag `v0.8.0`, tag-object SHA `d6613e0`)
**Previous release**: `003d1fe` (`v0.7.0`)
**GitHub release**: https://github.com/chodolmu/CJ-s-WorkTool/releases/tag/v0.8.0
**Diff scope**: `git diff v0.7.0..v0.8.0` — 19 files, +954 / −226 LOC.
**Reviewer scope**: git-based verification of the committed v0.8.0 state vs CHANGELOG / commit-message / HANDOFF / release-notes claims. No code changes proposed.

---

## 1. Verdict

**ACCURATE.**

Every Tier-1 / Tier-2 / M-1 deliverable claimed by the v0.8 CHANGELOG and commit message is present in the v0.8.0 commit at the file:line cited. The pre-flight script runs clean against the committed state (7/7 PASS, 0 warnings, exit 0). The 14-item Protocol 1 correction tally and 3-consecutive-ACCURATE claim are both verifiable from in-tree artifacts and git history.

Two **Minor** observations only — neither blocks the release nor contradicts a CHANGELOG claim:

- HANDOFF.md still reflects the *v0.8 work-start* state (cites `e084c78` as latest commit, status = "v0.8 사이클 Step 0 완료"), not the post-release state. The CHANGELOG does not claim HANDOFF was updated to post-release, so this is a process-hygiene gap rather than a dishonesty.
- Untracked empty `skills/scripts/` directory in working tree (not in git). Pre-existing local artifact, not in any v0.8 commit; pre-flight correctly reports `skills/ count: 29` because it counts `SKILL.md` files.

No **Critical** or **Major** defects. v0.8 makes it three consecutive ACCURATE releases under the Protocol 1 → 3 → 4 cycle.

---

## 2. CHANGELOG ↔ diff alignment

| CHANGELOG claim | Evidence in v0.8.0 commit | Verdict |
|---|---|---|
| Rule 17 (`pillars[].kind` enum read contract) added to `gmk-prototype-rules` with 4-case table | `skills/gmk-prototype-rules/SKILL.md:485-521` — new `## Rule 17 — pillars[].kind enum read contract (skill-wide pattern)` section with 4-row contract table (valid / absent / unknown enum / wrong casing) | PASS |
| 2 SKILL footers amended to "Rule 13-14, 16, 17" — only `gmk-prototype` + `gmk-self-test` | `skills/gmk-prototype/SKILL.md:26` and `skills/gmk-self-test/SKILL.md:42` are the *only* SKILLs with the new footer. The other 24 stay at "Rule 13-14, 16" (count via grep matches CHANGELOG). | PASS |
| Check G added to `check-plugin-meta.sh` (counts pillars in `pillars-example.json`, verifies each declares a valid `kind`) | `scripts/check-plugin-meta.sh:291-327` — new `Check G: pillars[].kind enum declaration (WARN-level in v0.8, Rule 17)` block; live run output: `pillars in example: 3, valid kind declarations: 3 — OK` | PASS |
| Check B promoted WARN → FAIL with whole-word regex `\bendpoint\b`, no `-i` | `scripts/check-plugin-meta.sh:147` header says *"FAIL-level in v0.8"*; line 172 uses `grep -rnIwE 'endpoint'` (no `-i`); endpoint_hits feeds `drift` not `warn` (line 180) — would exit 1 on drift. | PASS |
| `.endpoint-allowlist.txt`: stale `marketplace.json:8` entry removed | Diff shows the `:8` entry deleted; remaining entries all carry `verified-at: <SHA>` stamps; only `marketplace.json:15` survives. | PASS |
| `verified-at: <SHA>` stamps added to both allowlists | `.endpoint-allowlist.txt:10-23` header documents the convention; 6 entries carry `verified-at: e084c78`. `.rule14-allowlist.txt:18-31` header + 9 entries with `verified-at: e084c78` or `HEAD (2026-05-18, v0.8)`. | PASS |
| Allowlist staleness convention caught real drift: `gmk-art-gen/SKILL.md` line 140 → 144 | One `.rule14-allowlist.txt` entry stamped `verified-at: HEAD (2026-05-18, v0.8)` (i.e., re-verified during this release), corroborating CHANGELOG line 34's claim. | PASS |
| `templates/prototype-shader.html`: 401 → 299 lines | `wc -l templates/prototype-shader.html` returns **299**. Diff stats: `222 +/− lines` change, net negative; CHANGELOG figure matches exactly. Check E in pre-flight reports OK (under soft cap). | PASS |
| Bot hook API surface preserved (5 canonical + 2 optional) | `templates/prototype-shader.html:271-294` — `reset`, `isOver`, `legalActions`, `apply`, `collectSummary` (5 canonical) + `stateSignature`, `progressEstimate` (2 optional) + explicit comment that `riskEstimate`/`noveltyScore` are intentionally omitted with rationale. API version anchor preserved via `<script src="./_bot_hook_lib.js">` at line 57 (Check F satisfied). | PASS |
| `gmk-art-gen` Step 6 — mock cleanup reminder added | `skills/gmk-art-gen/SKILL.md:127-129,150-151` — detection logic (`test -f prototypes/${milestone_id}-mocked.html`) and reminder text added; informational only, no auto-delete. | PASS |
| `gmk-mock-inject` "What this skill does NOT do" cross-references the new trigger | `skills/gmk-mock-inject/SKILL.md:284` — new line: *"v0.8: `/gmk-art-gen` prints a one-line reminder in its Step 6 summary when a mocked sibling exists — the reminder is informational, not a destructive auto-cleanup."* | PASS |
| `pillars-example.json` `_comment` mentions Rule 17 alongside Rule 16 | Diff shows the `_comment` field updated; kit_version bumped to 0.8.0. | PASS |
| `gmk-init/SKILL.md` schema example includes `"kind": "sensory"`; new paragraph at L188 explains pillar kind semantics | `skills/gmk-init/SKILL.md` — diff shows the schema-example pillar gains a `kind` field; the new paragraph explains lowercase-exact-form requirement + wrong-casing warn+normalize behavior + Rule 17 link. | PASS |
| Version bumped 0.7.0 → 0.8.0 across all 5 meta sites | See §3 below. | PASS |
| `_workspace/handoff-backlog-template.md` NEW — falsifiable backlog template with file:line + grep query + non-targets + classification + defect-class link, plus Protocol-1-corrections retrospective field + F21 closure criterion (ratio < 1.0 × 3 releases) + tracking table seeded with v0.7 (1.75) and v0.8 (2.80) | New file in commit (114 lines). | PASS |
| Protocol 1 corrections: 14 items (6 ADD + 1 REMOVE + 4 VERIFY + 3 RECLASSIFY) + 3 missing | `_workspace/v0.8-protocol-1.md:231-243` Appendix subtotal table reads exactly **6 / 1 / 4 / 3 = 14 across 5 candidates + 3 missing**. (The §1 preliminary count of "10 + 2" was superseded by the final Appendix tally.) Matches CHANGELOG. | PASS |
| Ratio = 2.80 (corrections / candidate); v0.7 baseline = 1.75 | 14 corrections ÷ 5 candidates = 2.80; v0.7 was 7 ÷ 4 = 1.75 per the v0.7 CHANGELOG. Both numbers internally consistent. | PASS |
| Third consecutive ACCURATE release | v0.6 commit message (`bc4c7bc`) — first ACCURATE attestation. v0.7 commit message (`003d1fe`) — "v0.6 was the first ACCURATE release after 3 OVERSTATED rounds … Protocol 3 graded ACCURATE". v0.8 commit message (`b0a59c0`) — "Third consecutive ACCURATE release under the Protocol 1/3/4 evaluator cycle". Chain attestation consistent across 3 release commits. *Note*: Protocol 4 reports for v0.6 and v0.7 are not committed in `_workspace/` (only v0.8's reports are), so the ACCURATE chain rests on commit-message + CHANGELOG attestation rather than committed Protocol 4 artifacts. Documented but not flagged — the attestation chain is internally consistent and the verdict aligns with the current Protocol 4 run. | PASS (with note) |
| No new SKILLs / agents (count = 29 + 4) | `ls skills/ | grep -v '^scripts$' | wc -l` = 29; `ls agents/ | wc -l` = 4. Pre-flight `skills/ count: 29` (it counts SKILL.md files via glob, so the empty `skills/scripts/` working-tree dir doesn't inflate it). | PASS |
| Protocol 1 + Protocol 3 reports committed | `_workspace/v0.8-protocol-1.md` (243 lines) + `_workspace/v0.8-protocol-3.md` (committed) both in v0.8.0 commit. | PASS |

**Total: 18 CHANGELOG-claim checks, 18 PASS, 0 FAIL, 0 NOTED.**

---

## 3. Plugin meta consistency

| Site | Expected | Actual | Verdict |
|---|---|---|---|
| `.claude-plugin/plugin.json:3` `version` | `0.8.0` | `0.8.0` | PASS |
| `.claude-plugin/marketplace.json:9` outer `metadata.version` | `0.8.0` | `0.8.0` | PASS |
| `.claude-plugin/marketplace.json:16` `plugins[0].version` | `0.8.0` | `0.8.0` | PASS |
| `_workspace/examples/pillars-example.json:3` `kit_version` | `0.8.0` | `0.8.0` | PASS |
| `_workspace/examples/milestones-example.json:3` `kit_version` | `0.8.0` | `0.8.0` | PASS |
| `skills/gmk-init/SKILL.md:163` schema example `kit_version` | `0.8.0` | `0.8.0` | PASS |
| `skills/gmk-init/SKILL.md:196` schema example `kit_version` | `0.8.0` | `0.8.0` | PASS |

**7/7 consistent.** No version drift across meta sites — Check D in the pre-flight verifies this mechanically and reports OK.

---

## 4. Pre-flight result

Ran `bash scripts/check-plugin-meta.sh` against HEAD (`b0a59c0`). Output summary:

```
plugin.json version:       0.8.0
marketplace.json version:  0.8.0
skills/ count:             29
plugin.json description mentions: 29 skills

Check A: Rule 14 token presence (FAIL-level in v0.7) — OK
Check B: 'endpoint' terminology drift (FAIL-level in v0.8) — OK
Check C: Rule 13-14 citation footer (FAIL-level in v0.7) — OK
Check D: kit_version consistency in examples (WARN-level in v0.7) — OK
Check E: Template line-cap (WARN-level in v0.7, hard cap 600) — OK
Check F: Template __gmk_botHook__ API version (WARN-level in v0.7) — OK
Check G: pillars[].kind enum declaration (WARN-level in v0.8, Rule 17) — OK
  pillars in example: 3, valid kind declarations: 3

OK — plugin meta sanity checks pass.
EXIT_CODE=0
```

**Result: 7/7 checks PASS. 0 warnings. 0 drift. Exit code 0.** This is the first release where all 7 checks return OK with zero warnings (v0.7 baseline carried a Check E WARN on the 401-line shader template). Check B's first run as FAIL-level surfaces no drift, validating the v0.8 Protocol 1 audit's prediction that the regex tightening + allowlist cleanup would land cleanly.

---

## 5. Defects

### Critical
None.

### Major
None.

### Minor

**M4-1 — Post-release HANDOFF.md is stale (still v0.8 work-start state)**
- File: `HANDOFF.md:1-9`
- Observation: HANDOFF still reads *"v0.8 Protocol 1 완료, backlog 보정됨"* with latest commit cited as `e084c78` (the v0.7 post-release commit) and status = "v0.8 사이클 Step 0 (Protocol 1 work-start evaluator) 완료". The v0.8.0 release commit and tag are not reflected. Compare v0.6/v0.7 which both shipped a post-release HANDOFF update in a follow-up commit (`104dcae`, `e084c78`).
- Severity: Minor. The CHANGELOG does not claim HANDOFF was updated to post-release state in this commit, so there is no dishonesty. The gap is a process-hygiene matter — the v0.8 cycle convention would be a follow-up `docs(gamemaker-kit): post-v0.8 HANDOFF — third consecutive ACCURATE release reflection` commit, mirroring v0.6 and v0.7.
- Not release-blocking.

**M4-2 — Untracked empty `skills/scripts/` directory in working tree**
- Path: `skills/scripts/` (working tree only — `git ls-tree HEAD skills/` does not list it)
- Observation: An empty `scripts` directory exists inside `skills/`. It is not tracked in v0.8.0 (or any prior tag); it appears to be a local artifact from a prior session. `ls skills/ | wc -l` returns 30 because of it, which can mislead anyone counting directories rather than `SKILL.md` files.
- Severity: Minor. The pre-flight script counts `SKILL.md` files (correct count: 29), so Check D / D's downstream consistency checks are unaffected. This is a working-tree cleanup item, not a release defect.
- Not release-blocking.

---

## 6. 3-consecutive-ACCURATE confirmation

**Y — confirmed.**

Chain of attestations (all verifiable from git commit messages, all consistent):

| Release | Commit | Attestation source | Verdict claimed |
|---|---|---|---|
| v0.6.0 | `bc4c7bc` | Commit body: *"v0.5 declared … OVERSTATED for the third release in a row … v0.6 closes both sweeps … Protocol 4 (post-release) verdict: ACCURATE"* | First ACCURATE |
| v0.7.0 | `003d1fe` | Commit body: *"v0.6 was the first ACCURATE release after 3 OVERSTATED rounds. v0.7 keeps the 3-checkpoint evaluator process … Protocol 3 (pre-release) graded ACCURATE … Protocol 4 (post-release git) verdict: ACCURATE"* | Second ACCURATE |
| v0.8.0 | `b0a59c0` | Commit body: *"Third consecutive ACCURATE release under the Protocol 1/3/4 evaluator cycle."* | Third ACCURATE — this report confirms |

This Protocol 4 run grades v0.8 ACCURATE (18 CHANGELOG-claim checks PASS, 7/7 pre-flight PASS, 7/7 version consistency PASS, 0 Critical/Major defects). The chain is unbroken at three.

A note on falsifiability: Protocol 4 reports for v0.6 and v0.7 are not committed under `_workspace/` (only v0.8's Protocol 1 and Protocol 3 are). The ACCURATE chain is therefore attested-but-not-archived for the first two links. The chain is internally consistent (each release's commit message references the prior verdict, and the current Protocol 4 result aligns), so this is documented rather than flagged. A future improvement would be to commit Protocol 4 reports alongside Protocol 1 + Protocol 3 — but that's a process suggestion for v0.9, not a v0.8 defect.

---

## Summary

- **Verdict**: ACCURATE
- **Defects**: 0 Critical, 0 Major, 2 Minor (post-release HANDOFF staleness, untracked empty `skills/scripts/`)
- **Pre-flight**: 7/7 PASS, 0 warnings, exit 0
- **3-consecutive-ACCURATE**: Y (v0.6 → v0.7 → v0.8)
- **Process health**: F21 (HANDOFF anchoring) repeated for the third consecutive cycle but mildly improving; new `_workspace/handoff-backlog-template.md` is the structural counter-measure with a falsifiable closure criterion (ratio < 1.0 × 3 releases). v0.8 baseline ratio = 2.80, v0.7 = 1.75 — the rising number reflects deeper Protocol 1 scrutiny rather than worse authorship, per CHANGELOG line 13.
