# v0.9 Protocol 4 — Post-release evaluator report

**Date**: 2026-05-18 (21:42 KST)
**HEAD**: `c35eeae4089282d09e6f81d6878f3a3b5628abc2` (= annotated tag `v0.9.0`)
**Previous release**: `b0a59c0` (`v0.8.0`)
**GitHub release**: https://github.com/chodolmu/CJ-s-WorkTool/releases/tag/v0.9.0
**Diff scope**: release commit `c35eeae` — 17 files, +1161 / −68 LOC.
**Reviewer scope**: git-based verification of the committed v0.9.0 state vs CHANGELOG / commit-message / HANDOFF / release-notes claims. No code changes proposed.

---

## 1. Verdict

**ACCURATE.**

Every Tier-1 / Tier-2 deliverable claimed by the v0.9 CHANGELOG and commit message is present in the v0.9.0 commit at the file:line cited. The pre-flight script runs clean against the committed state (7/7 checks PASS, 0 warnings, exit 0 — the same 0-warning baseline v0.8 established). All 7 plugin-meta version-stamp sites declare `0.9.0` consistently (Check D verifies mechanically). The 18-item Protocol 1 correction tally, the 100% template-conformance claim, the `_save_breaking` → 0-mentions claim, and the 4-consecutive-ACCURATE chain claim are all verifiable from in-tree artifacts and git history. The Tier-3 deferral (Check G WARN→FAIL promotion, Rule 14 line-cap synthetic fixture) is honored — neither `scripts/check-plugin-meta.sh` nor any Rule 14 fixture appears in the release commit's diff.

Three **Minor** observations only — none blocks the release or contradicts a CHANGELOG claim:

- HANDOFF.md `Status` line in the release commit reflects the *pre-release* state (*"v0.9 사이클 Step 0 완료"*), not the *post-release* state. This is consistent with v0.6/v0.7/v0.8 precedent: the post-release HANDOFF update is Step 10, scheduled as a forthcoming follow-up commit (compare `104dcae`, `e084c78`, `bd819d3`). The CHANGELOG does not claim HANDOFF was updated to post-release in this commit, so this is process-hygiene, not dishonesty.
- Untracked empty `skills/scripts/` directory in working tree persists from v0.8 (not in v0.9.0 tag either — same pre-existing local artifact noted in v0.8 Protocol 4 §5).
- Protocol 4 reports for v0.6 and v0.7 still not committed under `_workspace/` (only v0.8's and now v0.9's are). v0.8 Protocol 4 documented this same gap honestly; it persists into v0.9 without change. Documented, not flagged.

No **Critical** or **Major** defects. v0.9 makes it **four consecutive ACCURATE releases** under the Protocol 1 → 3 → 4 cycle.

---

## 2. CHANGELOG ↔ diff alignment

| CHANGELOG claim | Evidence in v0.9.0 commit | Verdict |
|---|---|---|
| Rule 16 §Exercise paths annotation added between Rule 16's `### Why this rule exists` and Rule 17 | `skills/gmk-prototype-rules/SKILL.md:483-497` — new `### Exercise paths (live vs safety valve, as of v0.9)` subsection with 4-row contract table (Row 1 / 2 / 3 / 4), inserted between L481 (Why this rule exists closure) and the Rule 17 heading. +15 net lines per diff. | PASS |
| Annotation explicitly cites v0.7 Rule 14 CYCLE policy at `rule:388/398` | `skills/gmk-prototype-rules/SKILL.md:485` literal text: *"same shape as the Rule 14 CYCLE annotation at rule:388/398"*. | PASS |
| Annotation explicitly closes M-2 with Protocol 1 §2 narrative | `skills/gmk-prototype-rules/SKILL.md:497` literal text: *"This annotation closes v0.9's M-2 audit. The candidate originally proposed 4 dead branches…; Protocol 1 (v0.9-protocol-1.md §2) verified that `early_fail` is live (6 read sites in `gmk-validate`), `Math.random()` is a live guard pattern (5+ skills), and Rule 14 CYCLE was already closed in v0.7."* | PASS |
| `gmk-merge-gate:156` `_save_breaking` mention dropped (Path b) | `skills/gmk-merge-gate/SKILL.md:156` diff: warning text changed from *"…or confirm the change is non-breaking by adding a `_save_breaking: false` note to the milestone."* → *"…or confirm the change is non-breaking before merge."* | PASS |
| `grep -rn '_save_breaking' skills/` returns 0 hits after the change | Live `Grep` against `skills/` directory at HEAD returns **0 matches**. Full-tree grep returns matches only in CHANGELOG.md, HANDOFF.md, and `_workspace/` decision/protocol docs — all expected non-target locations. | PASS |
| README.md `~28 skills` → `29 skills` at line 6 | `README.md:6` diff: *"…~28 skills + 4 domain agents…"* → *"…29 skills + 4 domain agents…"* | PASS |
| README.md `## Skills (~28)` → `## Skills (29)` at line 43 | `README.md:43` diff: *"## Skills (~28)"* → *"## Skills (29)"* | PASS |
| README.md `\| Skills \| ~28 \| 72 \| n/a \|` → `\| Skills \| 29 \| 72 \| n/a \|` at line 192 | `README.md:192` diff confirms exact substitution. | PASS |
| CONCEPT.md `## 4. Skill matrix (v0.2 — ~28 skills, 4 domain agents)` → `## 4. Skill matrix (v0.2–0.9 — 29 skills, 4 domain agents)` at line 111 (living-doc policy) | `CONCEPT.md:111` diff confirms exact substitution with em-dash version-range form. | PASS |
| CONCEPT.md `\| Skills \| ~28 \| 72 \|` → `\| Skills \| 29 \| 72 \|` at line 409 | `CONCEPT.md:409` diff confirms exact substitution. | PASS |
| Only 2 surviving `~28` instances remain (CHANGELOG.md L373/L444 historical entries, explicit non-targets) | Live `Grep '~28'` returns only CHANGELOG.md historical entries (L453, L524 in current file = old L373/L444 plus shifted entries; both are inside v0.3/v0.2 release sections), HANDOFF.md backlog scoping notes, `_workspace/` Protocol docs, and the explicitly-named `extension-design-v0.3.DOGFOOD-DRIVEN.discarded.md`. **Zero `~28` in README.md or CONCEPT.md.** Matches CHANGELOG promise. | PASS |
| `_workspace/handoff-backlog-template.md:96-103` v0.9 row filled (6/18/3/3.00) | `handoff-backlog-template.md:102` diff: `\| v0.9 \| TBD \| TBD \| TBD \| TBD \|` → `\| v0.9 \| 6 \| 18 \| 3 \| 3.00 + 3 \|`. Matches CHANGELOG ratio claim exactly. | PASS |
| 5 NEW workspace files present | `git ls-tree v0.9.0` lists all 5: `_workspace/v0.9-protocol-1.md` (388 lines), `_workspace/v0.9-protocol-3.md` (206 lines), `_workspace/v0.9-gmk-port-split-decision.md` (86 lines), `_workspace/v0.9-save-breaking-decision.md` (85 lines), `_workspace/v1.0-backcompat-inventory.md` (133 lines). | PASS |
| `pillars-example.json` `_comment` mentions v0.9's Rule 16 safety-valve annotation | Diff shows the `_comment` field updated to include *"v0.9 annotates the three unreachable rows of Rule 16's contract as safety valves"*. | PASS |
| `milestones-example.json` `_comment` mentions Rule 16 annotation AND `_save_breaking` drop | Diff shows both clauses present: *"v0.9 made no schema changes"* + *"v0.9 dropped the `_save_breaking` field mention from gmk-merge-gate's warning text (0 readers → 0 mentions)"*. | PASS |
| `gmk-init/SKILL.md` schema example: `kit_version` bumped to `"0.9.0"` in BOTH example blocks | `skills/gmk-init/SKILL.md:163` and `:196` both read `"0.9.0"`. Diff confirms both substitutions. | PASS |
| `gmk-init/SKILL.md` prose anchor at L204 reflects v0.9 | `skills/gmk-init/SKILL.md:204` diff: *"v0.8 writes `\"0.8.0\"`"* → *"v0.9 writes `\"0.9.0\"`"*. | PASS |
| Plugin meta bumped to 0.9.0 across all 5 file sites (7 stamp sites total) | See §3 below — all 7 sites consistent. | PASS |
| Plugin descriptions updated to mention v0.9 deliverables | Both `plugin.json:4` description and both `marketplace.json:8/15` descriptions diff to add a v0.9 paragraph enumerating Rule 16 safety-valve annotation, `_save_breaking` drop, skill-count bump, ratio table fill, gmk-port KEEP decision, v1.0 backcompat inventory, and *"Fourth consecutive ACCURATE release"*. | PASS |
| Tier 3 candidates NOT touched (Check G promotion, Rule 14 line-cap exercise path) | `git diff v0.8.0..v0.9.0 --stat -- scripts/check-plugin-meta.sh` returns empty (Check G untouched, still WARN-level). No new Rule 14 fixture file appears in the v0.9.0 tree. Both correctly deferred. | PASS |
| Protocol 1 corrections: 18 items (7 ADD + 3 REMOVE + 6 VERIFY + 2 RECLASSIFY) + 3 missing candidates; ratio 3.00 | CHANGELOG line 15 declares exactly this; the v0.9-protocol-1.md report at `_workspace/v0.9-protocol-1.md:374` Appendix tracks the same breakdown. Internally consistent. (Protocol 1 doc is committed, so the claim is auditable in-tree.) | PASS |
| Protocol 3 verdict: ACCURATE, 100% template conformance on 6 in-scope candidates | `_workspace/v0.9-protocol-3.md` committed in v0.9.0 tree; §4 of that report tallies 6/6 candidates at 4/4 template fields each. Internally consistent with CHANGELOG line 11's claim. | PASS |
| "Fourth consecutive ACCURATE release" attestation | Commit message body: *"v0.6 was the first ACCURATE release, v0.7 the second, v0.8 the third. v0.9 targets a fourth consecutive ACCURATE release…"* — claim verified by §6 below. | PASS |
| No new SKILLs / agents (count stays at 29 + 4) | Pre-flight reports `skills/ count: 29`. `git diff v0.8.0..v0.9.0` shows no new SKILL.md files or agent files added. | PASS |

**Total: 23 CHANGELOG-claim checks, 23 PASS, 0 FAIL, 0 NOTED.**

---

## 3. Plugin meta consistency

| Site | Expected | Actual | Verdict |
|---|---|---|---|
| `.claude-plugin/plugin.json:3` `version` | `0.9.0` | `0.9.0` | PASS |
| `.claude-plugin/marketplace.json:9` outer `metadata.version` | `0.9.0` | `0.9.0` | PASS |
| `.claude-plugin/marketplace.json:16` `plugins[0].version` | `0.9.0` | `0.9.0` | PASS |
| `_workspace/examples/pillars-example.json:3` `kit_version` | `0.9.0` | `0.9.0` | PASS |
| `_workspace/examples/milestones-example.json:3` `kit_version` | `0.9.0` | `0.9.0` | PASS |
| `skills/gmk-init/SKILL.md:163` schema example `kit_version` | `0.9.0` | `0.9.0` | PASS |
| `skills/gmk-init/SKILL.md:196` schema example `kit_version` | `0.9.0` | `0.9.0` | PASS |

**7/7 consistent.** No version drift across meta sites — Check D in the pre-flight verifies this mechanically and reports OK.

---

## 4. Pre-flight result

Ran `bash scripts/check-plugin-meta.sh` against HEAD (`c35eeae`). Output:

```
plugin.json version:       0.9.0
marketplace.json version:  0.9.0
skills/ count:             29
plugin.json description mentions: 29 skills

Check A: Rule 14 token presence (FAIL-level in v0.7)
  OK — every SKILL with refuse-with-rec carries a [Rule 14 token (or is line-allowlisted)

Check B: 'endpoint' terminology drift (FAIL-level in v0.8)
  OK — no unexpected 'endpoint' occurrences in live docs

Check C: Rule 13-14 citation footer (FAIL-level in v0.7)
  OK — every SKILL with ## Preconditions carries the Rule 13-14 footer

Check D: kit_version consistency in examples (WARN-level in v0.7)
  OK — examples declare kit_version matching plugin.json MAJOR.MINOR

Check E: Template line-cap (WARN-level in v0.7, hard cap 600)
  OK — all templates under soft cap (300 lines)

Check F: Template __gmk_botHook__ API version (WARN-level in v0.7)
  OK — all templates with __gmk_botHook__ have an API version anchor

Check G: pillars[].kind enum declaration (WARN-level in v0.8, Rule 17)
  pillars in example: 3, valid kind declarations: 3
  OK — every pillar in the example declares a valid kind

OK — plugin meta sanity checks pass.
EXIT=0
```

**Result: 7/7 checks PASS. 0 warnings. 0 drift. Exit code 0.** This is the second consecutive release at the 0-warning baseline (v0.8 was the first; v0.9 maintains it). Check D correctly registers the 0.9.0 stamps in the schema-example files against the 0.9.x kit. Check B remains green after the v0.8 whole-word tightening — no `endpoint` drift introduced by the v0.9 description rewrites (verified despite the new v0.9 paragraphs being added to plugin.json and marketplace.json descriptions). Check G's 3/3 kind-declaration count is unchanged from v0.8 (no pillar-kind surface widening, consistent with the T3-A deferral rationale in CHANGELOG line 78).

---

## 5. Defects

### Critical
None.

### Major
None.

### Minor

**M4-1 — Pre-release HANDOFF.md state in the release commit, not post-release state**
- File: `HANDOFF.md:1-7`
- Observation: HANDOFF in the v0.9.0 release commit reads *"v0.9 사이클 Step 0 완료 (Protocol 1 UNDERSTATED, backlog 재작성됨)"* with latest commit cited as `bd819d3` (the v0.8 post-release commit) and status reflecting the v0.9 work-start state, not the v0.9 post-release state.
- Severity: Minor. This is consistent with the v0.6/v0.7/v0.8 release pattern — the post-release HANDOFF update is Step 10, landed as a forthcoming separate commit (`docs(gamemaker-kit): post-v0.9 HANDOFF — fourth consecutive ACCURATE release reflection`), mirroring `104dcae` / `e084c78` / `bd819d3`. The CHANGELOG does not claim HANDOFF was updated to post-release in this commit, so there is no dishonesty.
- Not release-blocking.

**M4-2 — Untracked empty `skills/scripts/` directory in working tree**
- Path: `skills/scripts/` (working tree only — `git ls-tree HEAD skills/` does not list it)
- Observation: Same pre-existing local artifact carried from v0.8 working tree. Not in any tracked commit; pre-flight correctly counts `SKILL.md` files (29) and is unaffected.
- Severity: Minor. Working-tree cleanup item, not a release defect. v0.8 Protocol 4 §5 flagged this as M4-2; v0.9 inherits the state unchanged.
- Not release-blocking.

**M4-3 — Protocol 4 reports for v0.6 and v0.7 still not committed**
- Path: `_workspace/v0.6-protocol-4.md`, `_workspace/v0.7-protocol-4.md` — both absent at HEAD.
- Observation: The ACCURATE chain for v0.6 and v0.7 remains attested-but-not-archived (only v0.8 and now v0.9 have committed Protocol 4 reports). v0.8 Protocol 4 §6 explicitly noted this gap and suggested committing future Protocol 4 reports alongside Protocol 1 + Protocol 3 — v0.9 honors that suggestion forward (this report will be committed in the post-release HANDOFF commit), but does not retroactively backfill v0.6/v0.7.
- Severity: Minor. The chain attestation is internally consistent (each release's commit body references the prior verdict), and v0.9's Protocol 4 (this report) independently grades the v0.9 commit ACCURATE — so the chain holds on the strength of in-tree artifacts for the most recent 2 of 4 links. Backfilling v0.6/v0.7 reports would be reconstructive (re-running Protocol 4 against the v0.6.0/v0.7.0 tags now), not authentic to those releases' wall time. Documented, not flagged.
- Not release-blocking.

### Sanity scans performed (all clean)

- `grep -rn '_save_breaking' skills/` → **0 hits**. The field is no longer part of the kit's live SKILL surface. CHANGELOG line 55's promise satisfied.
- `grep -rn '_save_breaking' .` → matches only in `CHANGELOG.md` (historical entries + v0.9 description), `HANDOFF.md` (v0.9 backlog notes), `_workspace/` (decision/protocol docs), and `.claude-plugin/{plugin,marketplace}.json` descriptions (where the v0.9 description text reads *"closes the last live half-applied field _save_breaking by dropping its single mention"* — describing the drop, not reintroducing the field). No live-surface reintroduction.
- `grep -n '~28\|28 skills' README.md CONCEPT.md` → 0 hits in both files (all 5 sites bumped). The only surviving `~28` instances in the repo are CHANGELOG historical entries (v0.2/v0.3 release sections, explicit non-targets per HANDOFF.md L181), HANDOFF.md scoping references, `_workspace/` Protocol docs documenting the bump, and the explicitly-discarded `extension-design-v0.3.DOGFOOD-DRIVEN.discarded.md`.
- `grep -n 'kit_version' _workspace/examples/*.json skills/gmk-init/SKILL.md` → all 4 sites declare `0.9.0`; Check D mechanically verifies against `plugin.json:3` MAJOR.MINOR.
- Rule 16 §Exercise paths annotation table internally consistent with the 4-row contract above it: Row 2 status = *Live*, rows 1/3/4 = *Safety valve, last-exercised: never*. The annotation does not contradict any contract-table cell (verified by reading both tables back-to-back at `skills/gmk-prototype-rules/SKILL.md:458-497`).
- CONCEPT.md line 111 wording *"v0.2–0.9 — 29 skills, 4 domain agents"* uses an en-dash version range, internally consistent with the document's living-doc treatment (the choice was explicitly justified in CHANGELOG line 48 with the *"versioned matrix range, not v0.2-anchored snapshot"* policy rationale).
- gmk-port SKILL.md not touched in the v0.9 diff (T2-A KEEP decision honored — file remains at its pre-v0.9 size; the decision artifact `_workspace/v0.9-gmk-port-split-decision.md` documents 5 re-open triggers for future cycles).

---

## 6. 4-consecutive-ACCURATE confirmation

**Y — confirmed.**

Chain of attestations (all verifiable from git commit messages, all consistent):

| Release | Commit | Attestation source | Verdict claimed |
|---|---|---|---|
| v0.6.0 | `bc4c7bc` | Commit body: *"v0.5 declared … OVERSTATED for the third release in a row … v0.6 closes both sweeps … Protocol 4 (post-release) verdict: ACCURATE"* | First ACCURATE |
| v0.7.0 | `003d1fe` | Commit body: *"v0.6 was the first ACCURATE release after 3 OVERSTATED rounds. v0.7 keeps the 3-checkpoint evaluator process … Protocol 4 (post-release git) verdict: ACCURATE"* | Second ACCURATE |
| v0.8.0 | `b0a59c0` | Commit body: *"Third consecutive ACCURATE release under the Protocol 1/3/4 evaluator cycle."* Confirmed by `_workspace/v0.8-protocol-4.md` (verdict: ACCURATE, 18 CHANGELOG checks PASS) | Third ACCURATE |
| v0.9.0 | `c35eeae` | Commit body: *"v0.6 was the first ACCURATE release, v0.7 the second, v0.8 the third. v0.9 targets a fourth consecutive ACCURATE release under the 4-checkpoint Protocol 1/3/4 cycle."* — this report (23 CHANGELOG checks PASS, 7/7 pre-flight PASS, 7/7 version consistency PASS, 0 Critical/Major defects) **confirms** | Fourth ACCURATE — this report confirms |

The chain is unbroken at four. Each release's commit body references the prior verdict, and each release's Protocol 4 result aligns with that attestation. The v0.9 Protocol 4 run grades v0.9 ACCURATE on the strength of in-tree evidence; v0.8 Protocol 4 graded v0.8 ACCURATE on the same basis.

**Falsifiability note (carrying forward from v0.8 Protocol 4 §6)**: Protocol 4 reports for v0.6 and v0.7 remain uncommitted; only v0.8's and now v0.9's are archived. The first two links of the chain rest on commit-message + CHANGELOG attestation alone. This is the same gap v0.8 Protocol 4 documented honestly; v0.9 inherits it without change. The trend going forward is: commit Protocol 4 alongside Protocol 1 + Protocol 3 (v0.9 honors this — this report will be committed in the post-release HANDOFF commit), and treat v0.6/v0.7 backfill as a separate decision (reconstructive vs. authentic). Documented, not a release defect.

**Process-health observations**:

- v0.9 is the **second consecutive 0-warning pre-flight release** (v0.8 was the first; v0.7 carried a Check E shader-template WARN).
- Protocol 1 corrections ratio: v0.7 = 1.75 → v0.8 = 2.80 → v0.9 = 3.00. Rising. CHANGELOG line 17 interprets this as deeper Protocol 1 scrutiny + new template-conformance dimension introduced in v0.8, not worse HANDOFF authorship. The new `_workspace/handoff-backlog-template.md` v0.9 row now reads `6 / 18 / 3 / 3.00 + 3` — internally consistent with both the CHANGELOG number and the v0.9-protocol-1.md tally.
- F21 closure criterion (ratio < 1.0 for 3 consecutive releases) not yet met. Realistic earliest closure per CHANGELOG line 19: v0.12.
- Template-conformance dimension (introduced v0.8 = 48% conformance on Protocol 3 candidates) climbs to 100% in v0.9 per Protocol 3 §4 — the template is being internalized one release after its introduction. Healthy.
- The half-applied field defect class (which spawned Rule 16 + Rule 17 in v0.7/v0.8) is **now empty**: v0.8 closed `pillars.kind`; v0.9 closed `_save_breaking`. CHANGELOG line 57's closure claim is verified by both the grep and the absence of any other declared-but-half-applied field in the v0.9 backcompat inventory at `_workspace/v1.0-backcompat-inventory.md`.

---

## Summary

- **Verdict**: **ACCURATE**
- **CHANGELOG checks**: 23 / 23 PASS
- **Plugin meta consistency**: 7 / 7 sites at `0.9.0`
- **Pre-flight**: 7 / 7 PASS, 0 warnings, exit 0 (second 0-warning baseline release)
- **Defects**: 0 Critical, 0 Major, 3 Minor (pre-release HANDOFF state in release commit per cycle convention, untracked empty `skills/scripts/` carried from v0.8, v0.6/v0.7 Protocol 4 backfill gap documented but not closed)
- **4-consecutive-ACCURATE**: Y (v0.6 → v0.7 → v0.8 → v0.9)
- **Process health**: Half-applied field defect class now empty (`pillars.kind` closed v0.8; `_save_breaking` closed v0.9). Template-conformance climbed 48% → 100% one release after introduction. Protocol 1 ratio rising (1.75 → 2.80 → 3.00) but CHANGELOG line 17 interprets as deeper scrutiny + template adoption, not worse authorship; predicted v0.10 ratio with template discipline internalized: 1.5–2.0. F21 closure target v0.12. v0.9 is the last v0.x release; v1.0 may deliberately break some of the guarantees enumerated in `_workspace/v1.0-backcompat-inventory.md`.
