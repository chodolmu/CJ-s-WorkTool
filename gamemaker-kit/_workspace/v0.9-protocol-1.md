# v0.9 Protocol 1 — Work-start evaluator report

**Date**: 2026-05-18 (21:07 KST)
**Anchor**: HANDOFF.md lines 52-90 (v0.9 backlog candidates), HEAD = `bd819d3` (post-v0.8 HANDOFF on top of `b0a59c0` v0.8.0 release).
**Reviewer scope**: scope-correction only, no v0.9 work performed.
**Template context**: First release using `_workspace/handoff-backlog-template.md`. v0.9 backlog is *partial-conformance* — HANDOFF written before template fully formalized.

---

## 1. Verdict

**UNDERSTATED.**

The HANDOFF v0.9 backlog (lines 52-90) proposes 6 candidates across 3 tiers. Major issues:

- **M-2 (declared-but-never-exercised audit) is OVER-SCOPED**: 3 of its 4 "dead branches" are NOT actually dead. Only `kit_version > current` is genuinely unreachable. `early_fail` is heavily read by `gmk-validate` (6+ sites). `Math.random()` warning is actively cited as a guard pattern by 5+ skills. Rule 14 CYCLE form was *intentionally* preserved as a "safety valve" in v0.7 (CHANGELOG L125-126) — re-auditing it constitutes re-litigating a closed decision. The candidate should be **reframed and narrowed**, not executed as written.
- **M-3 (README skill count) is UNDER-SCOPED**: HANDOFF cites `README.md:6` only. Reality: 3 sites in README (lines 6, 43, 192) + 2 sites in CONCEPT.md (lines 111, 409). Same defect class as v0.6 endpoint sweep — author's grep stopped at the first hit.
- **Tier 2 gmk-port split** has stale baseline: HANDOFF cites `prototype-rules 519` but the file is now **555 lines** (grew with Rule 17 addition in v0.8). The comparative ranking is intact but the number is wrong.
- **Tier 3 `_save_breaking: false` field convention** is correctly classified as half-applied (only mention site is gmk-merge-gate:156; no reader anywhere) but needs explicit Rule 17/Rule 16-style read contract decision, not just a "convention" note.
- **Missing candidates**: at least 3 — see §3. CONCEPT.md skill-count drift (couples with M-3 but distinct file), Rule 14 token line-cap enforcement currently dead (added in v0.7 but no SKILL.md.gmk-prototype currently triggers it), and v1.0 backward-compat audit setup (per HANDOFF L490 "v1.0 brainstorm" — this is the *last* v0.x release before MAJOR bump and there's no audit slot scheduled).

**Template-conformance score**: 6 candidates total. 4 fields per candidate required = 24 required field instances. Actual present: ~14/24 (58%). Major gaps: explicit non-targets absent in 5/6 candidates; defect-class link absent in 3/6. See §4.

**Total corrections proposed across all candidates**: 7 ADD + 3 REMOVE + 6 VERIFY + 2 RECLASSIFY = **18 items**, plus **3 missing candidates** flagged. Ratio = 18/6 = **3.0** (vs. v0.8 = 2.80, v0.7 = 1.75) — ratio still rising. See §5 for F21 interpretation.

---

## 2. Per-candidate corrections

### Candidate M-2 — Declared-but-never-exercised behaviors audit

**HANDOFF text**: 4 dead branches — Rule 14 CYCLE form / `kit_version > current` / `early_fail` field / `Math.random()` warning.

**Reality check** (executed in this session):

```
$ grep -rn 'early_fail' skills/
skills/gmk-validate/SKILL.md:20    ← reads & evaluates each row's early_fail condition
skills/gmk-validate/SKILL.md:63    ← --prune flag honors early_fail rules
skills/gmk-validate/SKILL.md:113   ← passes early_fail JSON to bot subprocess
skills/gmk-validate/SKILL.md:430   ← flag table documents early_fail dependence
skills/gmk-validate/SKILL.md:461   ← single-persona policy disables early_fail
skills/gmk-validate/SKILL.md:503   ← "pruned trial IS a FAIL — say early_fail rule tripped"
skills/gmk-prototype/SKILL.md:62/167/235 (examples + bot summary print)

$ grep -rn 'Math\.random' skills/
skills/gmk-portability-check/SKILL.md:60   ← port-risk pattern detector
skills/gmk-prototype-rules/SKILL.md:68     ← Rule (banned in game logic)
skills/gmk-prototype-rules/SKILL.md:79     ← non-determinism root-cause message
skills/gmk-prototype-rules/SKILL.md:93     ← reset() hook spec
skills/gmk-prototype-rules/SKILL.md:536    ← Pattern detection at validate time table row
skills/gmk-regression/SKILL.md:213         ← determinism break diagnostic
skills/gmk-validate/SKILL.md:39            ← determinism smoke check (#4)
skills/gmk-validate/SKILL.md:482           ← v0.1 prototype example
```

**REMOVE**:
- **`early_fail` is NOT dead.** Heavily read by `gmk-validate` (6 read sites) and example schema in `gmk-prototype:62/167/235`. The v0.4 schema audit `_workspace/v0.4-audit-schema.md:58-60` explicitly closed this as `~~SC-3~~ — confirmed normal`. Re-auditing it would re-litigate a closed finding.
- **`Math.random()` warning is NOT dead.** It is a *pattern detector* fired by `gmk-validate:39` (Step 1.iv determinism check) and `gmk-regression:213` (two-run divergence). The fact that "0 prototypes currently trip it" is *the intended state* — the kit's Rule 3 forbids it, so prototypes that comply don't trip. Same false framing as v0.7 Rule 14 CYCLE reframe — a defensive guard with 0 current trips is healthy, not dead.
- **Rule 14 CYCLE form was already reframed in v0.7.** CHANGELOG L125-126: *"CYCLE is preserved as the mandated form for future cycles that cannot be closed by widening either side. Currently unused — and that's the intended state."* Auditing it again *in v0.9* re-opens a closed decision. Drop from M-2.

**ADD**:
- After REMOVE pass, only **1 genuinely dead branch remains**: `kit_version > current` (Rule 16 row 3). Verify it's structurally unreachable in practice: every example file declares `kit_version: "0.8.0"`, no scenario in-repo produces a future-versioned file. Decide between (a) synthetic test invocation (e.g., add a `kit_version: "99.0.0"` test fixture that exercises the warn path) or (b) "safety valve, last-exercised: never" annotation in `gmk-prototype-rules:462`.
- **NEW dead-branch candidate the HANDOFF missed**: `kit_version` "absent" branch (Rule 16 row 1). Per `gmk-prototype-rules:460`, kit treats absent as `"0.3.0"`. Verify: do any in-repo files lack `kit_version`? `_workspace/examples/{pillars,milestones}-example.json` both declare 0.8.0. The "absent" path may also be never-exercised post-v0.4 since `gmk-init` always writes the field. **Add to audit scope.**
- **NEW dead-branch candidate**: `kit_version` "unparseable" branch (Rule 16 row 4). No fixture in repo. Same audit treatment.

**VERIFY**:
- Decide *the policy* once and apply to all 3 remaining `kit_version` branches: synthetic-fixtures-required vs. "safety valve" annotation. Mixing both styles in different rows is the v0.6 half-application defect class.

**RECLASSIFY**:
- **From `structural-guard` to `audit-only` (single-fix-per-branch)**: the candidate as written says "synthetic test invocation OR safety valve annotation" — this is documentation/fixture work, not adding a new Check H. Reframe scope: 1 branch (kit_version > current), or 3 branches (all 3 Rule 16 rows), but not "any never-exercised behavior across the kit."
- **Defect class link is wrong**: HANDOFF says *"declared-but-never-exercised (Rule 14 CYCLE pattern)"*. After the REMOVE above, the actual remaining defect class is *"future-version defensive code with no exercising fixture"* — a different, narrower class.

---

### Candidate M-3 — README skill count "~28 skills" → "29 skills"

**HANDOFF text**: "Targets: `README.md:6` (grep: `grep -n 'skills' README.md`). Non-targets: plugin.json/marketplace.json description."

**Reality check**:

```
$ grep -n '~28\|28 skills\|~29\|29 skills' README.md CONCEPT.md
README.md:6     ~28 skills + 4 domain agents covering 4 axes
README.md:43    ## Skills (~28)
README.md:192   | Skills | ~28 | 72 | n/a |
CONCEPT.md:111  ## 4. Skill matrix (v0.2 — ~28 skills, 4 domain agents)
CONCEPT.md:409  | Skills | ~28 | 72 |
```

5 live sites, not 1. HANDOFF's grep query `grep -n 'skills' README.md` would surface all 3 README hits — the author looked at one and stopped.

**ADD**:
- `README.md:43` — heading `## Skills (~28)`. Bump to `## Skills (29)`.
- `README.md:192` — comparison table row `| Skills | ~28 | 72 | n/a |`. Bump to `| Skills | 29 | 72 | n/a |`.
- `CONCEPT.md:111` — heading `## 4. Skill matrix (v0.2 — ~28 skills, 4 domain agents)`. Bump to `## 4. Skill matrix (v0.2-0.8 — 29 skills, 4 domain agents)` OR add a parenthetical "(now 29 as of v0.4)" — see VERIFY below for which.
- `CONCEPT.md:409` — comparison table row `| Skills | ~28 | 72 |`. Bump to `| Skills | 29 | 72 |`.

**REMOVE**:
- None. HANDOFF's one cited site is real.

**VERIFY**:
- `CONCEPT.md:111` says `(v0.2 — ~28 skills, 4 domain agents)`. The "v0.2" anchor is historically accurate (v0.2 *was* ~28). The fix is either (a) update to current ("v0.2-0.8 — 29 skills") or (b) keep historical and add a "current: 29" footnote elsewhere. Decide policy: is CONCEPT.md a living doc that tracks current count, or a v0.2-anchored architectural snapshot? Same question for CONCEPT.md:409.
- **HANDOFF says non-targets include "plugin.json/marketplace.json description"** — that's correct (both already say "29 skills" — verified in this session at plugin.json:4 and marketplace.json:8/15). Ratify that non-target list.
- **CHANGELOG.md:373 and :444 contain "~28"** in v0.3 and v0.2 release entries respectively. These are **historical Keep-a-Changelog entries — DO NOT touch** per the kit's v0.5+ convention (HANDOFF L181, CHANGELOG history frozen). Add to **explicit non-target list** in the candidate.

**RECLASSIFY**:
- HANDOFF tier is *Tier 1 — must-have*. Defect-class is implicitly "doc-drift on count metric." This is **single-fix** (one sed across 4 sites), not "sweep" — the change is mechanical, not policy. Classification is correct; no change.

---

### Tier 2 Candidate #3 — Protocol 1 corrections ratio tracking formalization

**HANDOFF text**: "Targets: `_workspace/handoff-backlog-template.md` 추적 표 + CHANGELOG `Process` 섹션 conventionalize."

**Reality check**:

- `_workspace/handoff-backlog-template.md:96-103` already contains the tracking table (v0.6 / v0.7 / v0.8 rows). v0.9 row marked TBD.
- CHANGELOG.md scanned: NO "Process" section exists in any release entry. v0.7 / v0.8 release entries do contain Protocol 1 corrections counts inline but not under a labeled section.

**ADD**:
- **Per-release Process section convention**: v0.9 CHANGELOG entry should introduce a `### Process — Protocol 1 retrospective` section with the count row. Format example per template:
  ```
  **Protocol 1 corrections**: <N> items (<X> ADD + <Y> REMOVE + <Z> VERIFY + <W> RECLASSIFY) + <M> missing candidates
  **Ratio**: <N/candidates> (vs. v0.8 = 2.80, v0.7 = 1.75)
  **F21 trend**: <interpretation>
  ```
- **Template self-update**: when v0.9 entry is written, also update the table in `handoff-backlog-template.md:96-103` to fill the v0.9 row.

**REMOVE**:
- None.

**VERIFY**:
- HANDOFF says CHANGELOG history is non-target ("Keep-a-Changelog 동결"). Correct. Retroactively adding Process sections to v0.7/v0.8 entries is *out of scope* — only forward (v0.9+) gets the convention.
- The template already declares the ratio formula and F21 closure criterion (`handoff-backlog-template.md:91-93`). Verify this candidate is *only* adding the CHANGELOG mirror, not duplicating the metric definition.

**RECLASSIFY**:
- Classification correctly listed as `process`. No change.
- **Defect-class link absent** in HANDOFF — add: *"F21 (HANDOFF anchoring)."*

---

### Tier 2 Candidate #4 — gmk-port 684-line split evaluation

**HANDOFF text**: "Targets: `grep -n '^### ' skills/gmk-port/SKILL.md`. Non-targets: prototype-rules 519, gmk-validate 503."

**Reality check**:

```
$ wc -l skills/gmk-port/SKILL.md skills/gmk-prototype-rules/SKILL.md skills/gmk-validate/SKILL.md
684 skills/gmk-port/SKILL.md
555 skills/gmk-prototype-rules/SKILL.md   ← HANDOFF says 519 (STALE)
503 skills/gmk-validate/SKILL.md
1742 total

$ find skills -name 'SKILL.md' | xargs wc -l | sort -rn | head -5
684 skills/gmk-port/SKILL.md
555 skills/gmk-prototype-rules/SKILL.md
503 skills/gmk-validate/SKILL.md
396 skills/gmk-self-test/SKILL.md
311 skills/gmk-merge-gate/SKILL.md

$ grep -n '^## ' skills/gmk-port/SKILL.md
21  ## Preconditions
43  ## Flow                              ← Stage 1 starts L58, Stage 6 ends L526
396 ## Stage 6 — what YOU re-verify by playing
413 ## DO NOT auto-translate — these need human re-tuning
435 ## Integration with existing project
443 ## What the bot validated (Stages 3-4 confirmed mostly intact)
602 ## Edge cases & policy
664 ## What this skill does NOT do
673 ## Notes for the model running this skill
```

**ADD**:
- **Stale-number correction**: HANDOFF says `prototype-rules 519` — actual is **555** (grew with Rule 17 in v0.8). Update HANDOFF text or accept that the comparison ranking holds regardless.
- **Concrete section-boundary list** for the split decision: `## Flow` (L43-396, 353 lines) is the largest section by far; it contains 6 Stages. A natural split candidate would be `gmk-port` (Stages 1-5) + `gmk-port-rerun` (Stage 6 re-validation + Edge cases + Notes), or extract `## DO NOT auto-translate` (L413-434, 22 lines) into a shared `gmk-port-translation-rules.md` reference doc.
- **Decision-or-keep**: the candidate should produce *either* a split spec *or* a one-line justification for keeping at 684 (e.g., "Stages 1-6 are operationally one flow; splitting forces the user to remember which sub-skill to call when").

**REMOVE**:
- None.

**VERIFY**:
- HANDOFF *Note* line: *"Rule 2 scope 미적용 (SKILL 문서)."* Correct per v0.8 commit message rationale (`gmk-prototype-rules:58` calibration is for prototypes). Ratify.
- Confirm `prototype-rules 555` is still under any hypothetical doc-cap. There is *no* doc line-cap rule today — this is a readability decision, not a compliance gate. The HANDOFF should make this explicit so a future evaluator doesn't propose adding a doc-cap rule.

**RECLASSIFY**:
- Classification listed as `single-fix (혹은 split 결정)`. The "or split" alternative makes it ambiguous. Recommend hardening to **decision-candidate**: produce a written decision artifact (split-or-keep) in `_workspace/v0.9-gmk-port-split-decision.md` *before* executing. If decision = keep, the candidate closes with the decision doc as the artifact. If decision = split, a follow-up sub-candidate executes the split.
- **Defect-class link absent** — add: *"none — this is a readability/maintenance candidate, not a defect."*

---

### Tier 3 Candidate — Check G WARN → FAIL promotion

**HANDOFF text**: "Check G WARN → FAIL 승격 검토 (baseline 정착 후)."

**Reality check**:

- Check G added v0.8 (`scripts/check-plugin-meta.sh:291-327`). Baseline: WARN-level.
- Live output (verified in this session): `pillars in example: 3, valid kind declarations: 3 — OK`. Check has fired 0 times since added.
- Precedent: Check B was added in v0.6 as WARN, promoted to FAIL in v0.8 (2 release cycles later). Check G is currently 1 release post-introduction.

**ADD**:
- **Promotion criterion**: HANDOFF says "baseline 정착 후" — vague. Recommend explicit criterion: *"Promote to FAIL after N consecutive 0-warning pre-flight runs across N+1 releases, where Check G has had a chance to fire."* Define N. v0.8 → v0.9 is only 1 cycle; suggests **DEFER to v1.0** (2 cycles minimum).

**REMOVE**:
- None.

**VERIFY**:
- Does v0.9 add any **new pillar-kind read site**? If yes, FAIL promotion is justified by the wider read surface area. If no, defer per the N-cycle rule above.
- Verify Check G's pillar-count detection is robust to schema variation (e.g., what if a user's `pillars.json` has 5 pillars and only 4 have `kind`?). Currently `pillars-example.json` only has 3 pillars, all with valid kind — the check has never been exercised against a real-world drift.

**RECLASSIFY**:
- HANDOFF tier is Tier 3 (defer). Correct. **Classification absent** — add: `structural-guard` (a Check in `check-plugin-meta.sh`).
- **Defect-class link absent** — add: *"declared-but-half-applied field (closure of v0.8 Rule 17)."*

---

### Tier 3 Candidate — `_save_breaking: false` field convention

**HANDOFF text**: "gmk-merge-gate L156 warning row에서 언급된 field — 실제 wire 없음. half-applied class 후보?"

**Reality check**:

```
$ grep -rn '_save_breaking' .
skills/gmk-merge-gate/SKILL.md:156   ← only mention site, in warning text
HANDOFF.md:89   ← this candidate itself
```

Confirmed: 1 mention site (gmk-merge-gate:156 warning text), 0 readers anywhere in the kit. The warning *suggests* the user add `_save_breaking: false` to the milestone, but no SKILL consumes that field once added.

**ADD**:
- **Decision required first**: is `_save_breaking` *meant* to be read by `gmk-save-migrate` (skipping the migration generation when set to `false`)? Or is it just a user-facing acknowledgment with no machine consumer? The current text in gmk-merge-gate:156 reads like a "either run /gmk-save-migrate OR add this field" alternative — implying the field has consumer semantics that don't exist.
- Two paths:
  - **(a) Make it read**: add a reader in `gmk-save-migrate` (precondition: if milestone has `_save_breaking: false`, skip migration plan generation with a noop record). This is a small Rule 17-style read contract.
  - **(b) Remove the suggestion**: change gmk-merge-gate:156 to drop the `_save_breaking` mention; the warning then says only "run /gmk-save-migrate or confirm the change is non-breaking" without giving the user a half-applied field name.

**REMOVE**:
- None.

**VERIFY**:
- Same defect class as `pillars.kind` was pre-v0.8 (declared in one site, no reader). HANDOFF correctly identifies this. Decide if v0.9 closes it (small Rule 18 add) or v1.0 brainstorm handles it.
- Verify by re-grep at v0.9 start: confirm no other SKILL has *quietly* added a read in interim work.

**RECLASSIFY**:
- HANDOFF tier is Tier 3 (defer). **Classification absent** — add: `single-fix` for path (a), or `process/doc-cleanup` for path (b).
- HANDOFF text ends with a question mark ("half-applied class 후보?"). Make the answer explicit: **YES, it is. Pick path (a) or (b) before v0.9 closes** — otherwise it carries into v1.0 as a known unresolved field convention.
- **Defect-class link** — add: *"declared-but-half-applied field."*

---

## 3. Missing candidates

### M-NEW-1 — CONCEPT.md skill-count drift (couples with M-3 but distinct file)

**Evidence**: `CONCEPT.md:111` and `CONCEPT.md:409` both contain "~28" (see M-3 §2 reality check). M-3 is scoped to README.md only ("README skill count"). CONCEPT.md is a separate file with its own historical-vs-current question (see M-3 VERIFY).

**Proposed candidate**: *"Decide CONCEPT.md skill-count policy — is CONCEPT.md a living current-state doc or a v0.2-anchored architectural snapshot? Apply decision to lines 111 and 409. Tier 1 if treated as a single sweep with M-3; Tier 2 standalone."*

**Defect class**: same as M-3 (doc-drift on count metric).

### M-NEW-2 — Rule 14 token at line-cap enforcement currently unexercised by gmk-prototype

**Evidence**: Check E (added v0.7, FAIL-level since baseline) enforces line caps on `templates/prototype-*.html`. The Rule 14 token `[Rule 14] /gmk-prototype → /gmk-prototype-rules` should fire on any *user-authored* prototype exceeding 300 lines. But:
- v0.8 successfully reduced the only over-cap template (`prototype-shader.html`) to 299 lines.
- No SKILL flow exists that *triggers gmk-prototype on a user's existing-over-cap prototype* to test the refuse path.
- `skills/gmk-prototype-rules:530-531` declares the policy ("hard refusal, soft warning") but no test path exercises it post-v0.8.

This is the **actual Rule 14 CYCLE-form defect class** the HANDOFF M-2 was reaching for: a declared rule with no current exercise. But unlike M-2's mostly-spurious 3 examples, this one is real.

**Proposed candidate**: *"Add a synthetic over-cap test fixture (`templates/_test_over_cap.html` at 301 lines) that the pre-flight script runs against `gmk-prototype` review to verify the refuse path fires. Or document the policy as a 'safety valve' with no expected exercise."*

**Defect class**: declared-rule-with-no-exercise-path (real one, M-2's framing applied correctly).

### M-NEW-3 — v1.0 backward-compat audit slot (HANDOFF L490 mentions but doesn't candidate-ize)

**Evidence**: HANDOFF Resume Instructions Step 5 (L488-490) says *"v1.0 MAJOR bump 시점 결정. backward-compat break 가능 (Rule 16 보장 종료) — v1.0 candidate audit 필요."* This is mentioned as a Step 5 but not added as a v0.9 backlog candidate. v0.9 is the **last v0.x release before v1.0**; if no audit happens in v0.9, v1.0 will have no inventory of what backward-compat guarantees exist to be broken.

**Proposed candidate** (Tier 2 — should-have): *"Produce `_workspace/v1.0-backcompat-inventory.md` — enumerate every Rule that promises backward-compat (Rule 16 row 2, additive-only schema across sub-releases, deprecated-field forgiveness from v0.4 hotfixes, etc.). Each entry: 'this is a v0.x guarantee that v1.0 may break.' Output is informational only — v0.9 doesn't break anything, but v1.0 needs the list to make breaking decisions deliberately rather than accidentally."*

**Defect class**: missing-release-readiness-artifact (analogous to v0.6's HANDOFF backlog vacuum that prompted F21).

---

## 4. Template conformance per candidate

Per `_workspace/handoff-backlog-template.md`, each candidate needs 4 fields: (1) file:line + grep query, (2) explicit non-targets, (3) classification, (4) defect-class link.

| Candidate | (1) file:line+grep | (2) non-targets | (3) classification | (4) defect-class | Score |
|---|---|---|---|---|---|
| M-2 (dead branches) | partial — listed 4 sites + grep query | absent | present (`structural-guard`) | present (`declared-but-never-exercised`) | 3/4 |
| M-3 (README count) | partial — 1 site cited (real has 5) + grep query | partial (mentions plugin.json/marketplace.json) | present (`single-fix`) | absent | 2.5/4 |
| Tier 2 #3 (Protocol 1 ratio) | partial — 1 file + 1 section | partial (CHANGELOG history) | present (`process`) | absent | 2.5/4 |
| Tier 2 #4 (gmk-port split) | present — file + grep query | partial (prototype-rules 519 [stale], gmk-validate 503) | partial (`single-fix 혹은 split 결정`) | absent | 2.5/4 |
| Tier 3 Check G promote | absent | absent | absent | absent | 0/4 |
| Tier 3 `_save_breaking` | partial — 1 file:line | absent | absent | absent | 1/4 |

**Aggregate**: 11.5/24 fields present = **48% template conformance**. This is below the implicit "first release with template" bar; expected closer to 80%. The two Tier 3 candidates were written in compressed prose (1-2 lines each) without template fields at all — those are the biggest gaps.

**Recommendation**: before v0.9 work starts, the HANDOFF author should fill the missing fields for all 6 candidates. The Protocol 1 evaluator (this report) has done that work; the author should fold those back into HANDOFF.md as the official backlog.

---

## 5. F21 trend interpretation

Current ratio: 18 corrections / 6 candidates = **3.0**. Trend: v0.7 = 1.75 → v0.8 = 2.80 → v0.9 = 3.0.

**This is NOT a worsening of HANDOFF authorship** — it's the expected effect of:
1. **Wider Protocol 1 scrutiny** with the new template — evaluator now grades against 4 mandatory fields rather than scope-only.
2. **Mixed-conformance HANDOFF** — half the candidates lack non-targets and defect-class links, which auto-generates RECLASSIFY/ADD corrections.
3. **Stale baseline numbers** in candidate descriptions (e.g., prototype-rules 519→555) — mechanical drift since HANDOFF was written.

**Predicted v0.10 ratio if HANDOFF authors write to template**: ~1.5-2.0. The corrections this report surfaces under §4 (template conformance) will not recur once authors adopt the template shape — those are one-time adoption costs.

**F21 closure target**: ratio < 1.0 for 3 consecutive releases. **Not achieved in v0.9**. Realistic earliest closure: v0.12 (after 2 cycles of template-discipline learning + 3 cycles below threshold). The kit should not panic about ratio rising in v0.9 — it's diagnostic, not regression.

---

## 6. Coupling notes

- **M-2 REMOVE pass first**: removing 3 false positives from M-2 narrows the candidate to "1 genuinely dead branch (`kit_version > current`) ± 2 unexercised Rule 16 rows." This is now a *small* candidate, not a sweep. Reframe before scheduling — the wrong shape will mislead the implementer.
- **M-3 + M-NEW-1 together**: both are doc-count drift. Treat as one sweep across `README.md` + `CONCEPT.md` (5 sites total), with one policy decision ("CONCEPT.md is living or snapshot?") feeding both.
- **Tier 3 Check G promotion depends on Tier 1 work**: if v0.9 adds new pillar-kind read sites (it likely won't), promotion is justified now. If not, defer to v1.0 by convention.
- **`_save_breaking` decision should happen before v1.0**: leaving it half-applied into v1.0 means v1.0 inherits the F21 defect class. Recommend v0.9 closes it (path (a) or (b)) rather than carrying it.
- **M-NEW-3 (v1.0 backcompat audit) is timing-critical**: v0.9 is the last v0.x. If skipped, v1.0 will have no inventory to break compat against deliberately.

---

## 7. Recommended v0.9 final scope

**Tier 1 (must-have)**:

1. **M-2 reframed** — narrow to 1-3 genuinely-unexercised Rule 16 rows (`kit_version > current`, optionally `kit_version` absent and unparseable). Apply *one* policy uniformly: synthetic fixture OR "safety valve" annotation. Drop early_fail / Math.random / Rule 14 CYCLE — these are not defects.
2. **M-3 + M-NEW-1 combined** — bump skill count across all 5 doc sites (`README.md:6/43/192`, `CONCEPT.md:111/409`). Decide CONCEPT.md living-vs-snapshot policy in the commit message.
3. **Tier 2 #3 reframed** — add the `### Process` section convention to v0.9's CHANGELOG entry; update the tracking table in `handoff-backlog-template.md:96-103`.

**Tier 2 (should-have)**:

4. **Tier 2 #4 with stale-number fix** — produce a written decision artifact for gmk-port split (split or keep), with the corrected baseline (prototype-rules 555, not 519).
5. **`_save_breaking` decision** — close as path (a) or (b). Don't carry into v1.0.
6. **M-NEW-3 v1.0 backcompat inventory** — informational artifact, no breaking changes in v0.9 itself.

**Tier 3 (defer to v1.0 unless trivially small)**:

7. **Check G WARN → FAIL** — defer to v1.0 (1-cycle baseline is too short).
8. **M-NEW-2 Rule 14 line-cap test fixture** — defer or fold into M-2 if M-2 picks the "synthetic fixture" policy.

**v0.9's one-line promise** (in HANDOFF Goal style): *"Close the last live half-applied field (`_save_breaking`), bump the stale skill-count across all 5 doc sites, narrow M-2 to its 1 genuinely-dead branch, and produce the v1.0 backward-compat inventory so v1.0 has a target list."*

---

## 8. Final verdict

**Protocol 4 prediction if HANDOFF executed AS-IS** (without this report's amendments): **OVERSTATED**.

Reasoning: M-2 as written would execute work on 3 non-defects (early_fail, Math.random, Rule 14 CYCLE), producing CHANGELOG claims that Protocol 4 git-diff verification would flag as "no actual change — these were already live/intentional". M-3 as written would close only 1/5 sites, leaving 4 silent failures across README+CONCEPT that the next Protocol 1 evaluator catches.

**Protocol 4 prediction AFTER this report's amendments applied**: **ACCURATE** (4 consecutive expected).

The scope-correction work here mirrors v0.8 Protocol 1 in style and severity — F21 caught again, fixed again, kit continues toward F21 closure with ratio trending and template adoption taking effect.

---

## Appendix — Numeric corrections summary

| Candidate | ADD | REMOVE | VERIFY | RECLASSIFY |
|---|---|---|---|---|
| M-2 declared-but-never-exercised | 2 | 3 | 1 | 1 |
| M-3 README skill count | 4 | 0 | 2 | 0 |
| Tier 2 #3 Protocol 1 ratio tracking | 1 | 0 | 1 | 0 (defect-class add only) |
| Tier 2 #4 gmk-port split | 0 (stale-number flag is VERIFY) | 0 | 1 | 1 |
| Tier 3 Check G promotion | 0 (promotion-criterion add is ADD) → 1 | 0 | 1 | 0 (classification + defect-class adds) |
| Tier 3 `_save_breaking` | 0 (decision-required is ADD) → 1 | 0 | 0 | 0 (classification + defect-class adds) |
| **Subtotal** | **7** | **3** | **6** | **2** |
| Missing M-NEW-1 (CONCEPT.md drift) | new candidate | | | |
| Missing M-NEW-2 (Rule 14 line-cap unexercised) | new candidate | | | |
| Missing M-NEW-3 (v1.0 backcompat inventory) | new candidate | | | |

**Grand total: 18 items across 6 candidates + 3 missing candidates.**

| Release | Candidates | Corrections | Missing | Ratio (corr/cand) |
|---|---|---|---|---|
| v0.7 | 4 | 7 | 0 | 1.75 |
| v0.8 | 5 | 14 | 3 | 2.80 |
| **v0.9** | **6** | **18** | **3** | **3.0** |

Ratio trend: rising. Interpretation: deeper scrutiny + new template-conformance dimension, not worse authorship (see §5).
