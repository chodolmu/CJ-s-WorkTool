# gamemaker-kit v0.4 Backlog — Skeleton Quality-of-Life

**Date**: 2026-05-15
**Source**: `_workspace/v0.4-skeleton-audit.md` — 3-view audit (cold-read / adversarial / schema). Not dogfood-driven (방침 확정).
**Scope**: v0.3 wiring 위에서 *남은 뼈대 결함* 정리. 신규 SKILL 0개 원칙 유지 (v0.3 유산).

---

## 0. v0.4의 한 줄 약속

> "**v0.3 wiring 위의 *quality-of-life*. orphan flag · precondition 일관성 · trace data 정리 · schema versioning · agent routing 표준화.**"

- v0.4 = 일관성 + 정합성. 새 기능 0, 새 SKILL 0, 새 agent 0.
- **breaking 위험 있음** — Wave γ에서 schema 정리 (write-only 9 필드 deprecate + `kit_version` 도입).
- backward-compat migration: `kit_version` 부재 시 v0.3로 간주 후 마이그레이션 권유.

---

## 1. 5개 결정 (확정)

| # | 결정 | 채택 옵션 |
|---|---|---|
| 1 | trace data (write-only 9 필드) | **Deprecate** — schema에서 제거, SKILL이 write 안 함, example.json 정리 |
| 2 | G3 precondition 위치 | **gmk-prototype-rules 확장** — 기존 룰북에 섹션 추가, 27 SKILL이 citation |
| 3 | G6 agent routing template | **prototype-rules 공통 block** — 1곳 정의, citation으로 표준화 |
| 4 | schema_version 도입 시점 | **v0.4 도입** (read 검증은 v0.5) — `kit_version` 필드 추가, optional |
| 5 | `--force-rebuild` semantics | **`--stage 1` alias** — hand-edit 손실 없이 spec만 재생성 |

---

## 2. v0.3 deferred 처리

| 코드 | 항목 | v0.4 처리 |
|---|---|---|
| **J** | save-migrate → merge-gate | Wave ε에서 *warning only* 와이어링 (v0.3 audit 결론 유지 — 신규 SKILL 안 함) |
| **O/P** | trials[]/validation_history[] write-only | Wave γ deprecate (결정 1과 통합) |

---

## 3. Wave 분해

### Wave α — Orphan flag 정리 (★★ G1, G2)

**목표**: 모든 referenced sub-flag가 *정식 정의 섹션*에 등록되도록.

| 작업 | 변경 위치 |
|---|---|
| α1. `gmk-validate` 본문에 **Sub-flags** 섹션 추가: `--rebaseline`, `--accept-regression`, `--policy`, `--prune`, `--runs`, `--confidence` 등 모든 flag의 (이름, 의미, 기본값, 부작용) 표 | `skills/gmk-validate/SKILL.md` |
| α2. `gmk-port` 본문에 동일한 **Sub-flags** 섹션. `--force-rebuild`는 *`--stage 1`의 alias*로 명시 (결정 5) | `skills/gmk-port/SKILL.md` |
| α3. 다른 SKILL의 sub-flag도 일관 표 형식 (gmk-self-test, gmk-kill-milestone, gmk-narrative, gmk-ux-flow, gmk-dev-complete) | 5 SKILL 본문 |
| α4. grep 검증: 본문에서 추천되는 모든 `--flag`이 *정의 섹션*에 잡힘 | CI / 수동 |

**완료 신호**: 본문 grep `--[a-z]` → *정의 섹션에 없는 flag*는 0.

### Wave β — Precondition / state 일관성 (★★ G3, AD-5, AD-9)

**목표**: 27 SKILL이 같은 방식으로 비정상 입력을 처리하도록.

| 작업 | 변경 위치 |
|---|---|
| β1. `gmk-prototype-rules`에 새 섹션 **Rule 13 — Milestone-id resolution + empty state**: id not found → list-and-stop / empty milestones.json → 안내 / partial state → graceful warn | `skills/gmk-prototype-rules/SKILL.md` |
| β2. 같은 룰북에 **Rule 14 — Refuse-chain cycle guard**: SKILL이 다른 SKILL을 추천할 때 그 SKILL의 precondition을 *함께* 노출 | 동 |
| β3. 27 SKILL 본문 Preconditions 섹션 끝에 한 줄 추가: *"See `gmk-prototype-rules` Rule 13-14 for milestone-id resolution and empty-state behavior."* | 27 SKILL |
| β4. AD-5 cycle (self-test→validate→self-test 등) 점검: Rule 14 적용 후 cycle 시나리오에서 어디서 stop하는지 명시 | 영향 SKILL 본문 |

**완료 신호**: 27 SKILL이 Rule 13-14 reference 가짐 + cycle 시나리오 본문 명시.

### Wave γ — Schema 정리 (★★ G4, G5, SC-1, SC-9, **breaking**)

**목표**: schema의 write-only 9 필드 deprecate + versioning 도입 + example 정합성.

| 작업 | 변경 위치 |
|---|---|
| γ1. `milestones.json` 루트에 `kit_version: "0.4.0"` optional 필드 추가 (결정 4). 없으면 v0.3로 추정 | gmk-init / structure.md |
| γ2. write-only 9 필드 deprecate (결정 1): `hypothesis.trials[]`, `validation_history[]`, `re_validation_history[]`, `kill_history[]`, `kill_category`, `kill_followup`, `validation.guardrails`, `coded_themes`, `self_test.sessions[].*` body | gmk-validate, gmk-regression, gmk-port, gmk-kill-milestone, gmk-self-test 본문 — write 코드 제거 |
| γ3. example.json 갱신: write-only 필드 제거 + `_kill_reason` → `kill_reason` 정정 (SC-2) | `_workspace/examples/milestones-example.json` |
| γ4. **pillars-example.json 신규 작성** (SC-4) | `_workspace/examples/pillars-example.json` |
| γ5. structure.md에 추가: path convention (SC-6), policy enum (SC-7), `merge_gate.touched_files` 형식 (SC-8), `metrics.custom` 사용처 (SC-10) | `_workspace/structure.md` |
| γ6. port checklist frontmatter 정의 (SC-5) | `skills/gmk-port/SKILL.md` |
| γ7. **Migration note** — v0.3 사용자가 v0.4 install 시 write-only 필드 자동 제거 정책 (보존 vs 삭제) | CHANGELOG / `gmk-init --migrate` flag 검토 |

**완료 신호**: example.json grep으로 write-only 필드 0건. structure.md 완전. pillars-example.json 존재.

### Wave δ — Agent routing 표준화 (★★ G6, CR-7, AD-10)

**목표**: 4 agents reference하는 SKILL이 *같은 형식*으로 routing 추천.

| 작업 | 변경 위치 |
|---|---|
| δ1. `gmk-prototype-rules`에 새 섹션 **Rule 15 — Agent routing block**: 표준 template (`Next agent (optional): @<name> <milestone-id> — <one-line reason>`) | `skills/gmk-prototype-rules/SKILL.md` |
| δ2. 4 agents의 precondition을 룰북 Rule 15에 *함께* 명시 (AD-10 처리) | 동 |
| δ3. agent reference 가진 6 SKILL이 Rule 15 citation으로 출력 형식 통일 | gmk-design-system, gmk-prototype, gmk-content-plan, gmk-validate, gmk-self-test, gmk-regression, gmk-port, gmk-loop |
| δ4. CR-6 (`--bot-only` 의미 모호) 같은 SKILL에서 함께 명문화 | `skills/gmk-prototype/SKILL.md` |

**완료 신호**: 6 SKILL 본문에서 agent routing 출력 형식이 grep으로 동일 pattern.

### Wave ε — 운영 / 마이너 정리 (★)

| 작업 | 변경 위치 |
|---|---|
| ε1. lock 자동 만료 + PID 검증 (CR-8 + AD-6) | `skills/gmk-loop/SKILL.md` |
| ε2. plugin meta auto-sync script (CR-9) | `scripts/check-plugin-meta.sh` 신규 |
| ε3. dashboard archive flag (CR-10) | `skills/gmk-status/SKILL.md` |
| ε4. shader template "Wave D" stale 카피 정정 (CR-3) | `templates/prototype-shader.html` + `skills/gmk-prototype/SKILL.md` |
| ε5. `--force` semantics 명문화 (AD-4) | 7 영향 SKILL |
| ε6. refuse 메시지 영어 통일 명시 (AD-7) | prototype-rules Rule 13 옆에 추가 |
| ε7. `--accept-warnings` persist 처리 (AD-8 / W20) | `skills/gmk-dev-complete/SKILL.md` + milestones.json `warnings_acknowledged_at` |
| ε8. portability-check JSON persist (CR-2) | `skills/gmk-portability-check/SKILL.md` |
| ε9. save-migrate → merge-gate warning-only wiring (J carry-over) | `skills/gmk-merge-gate/SKILL.md` |
| ε10. hand-edit 정책 명시 (CR-5) | CONCEPT.md / README.md |
| ε11. CR-4 (future skill 약속) 정리 | 2 SKILL 본문 — "future" 표현 삭제 또는 정직화 |

### Wave ζ — 릴리스 메타

| 작업 | 변경 위치 |
|---|---|
| ζ1. CHANGELOG.md v0.4 섹션 — Wave α-ε 변경 + migration notes | `CHANGELOG.md` |
| ζ2. plugin.json + marketplace.json v0.4.0 | `.claude-plugin/*.json` |
| ζ3. HANDOFF.md 갱신 | `HANDOFF.md` |
| ζ4. v0.4 audit 자료 (audit md 4개) 보존 또는 정리 | `_workspace/v0.4-*.md` |

---

## 4. 우선순위 권고

| Wave | 비용 | 가치 | 권고 순서 |
|---|---|---|---|
| α (orphan flag) | 작음 | 큼 (사용자 막힘 해소) | **1** |
| β (precondition) | 중간 | 큼 (27 SKILL 일관성) | **2** |
| δ (agent routing) | 작음 | 중간 (v0.3 wiring 완성) | **3** |
| γ (schema) | 큼 (breaking) | 큼 (정합성) | **4** |
| ε (운영) | 중간 | 작음 (개별 결함) | **5** |
| ζ (릴리스) | 작음 | — | **6** |

α → β → δ → γ → ε → ζ 순서. γ를 *마지막에 가까이* 두는 이유: breaking이라 migration script 작성 + 사용자 안내 비용이 큼.

---

## 5. 비목표 (v0.4가 *하지 않는 것*)

| 항목 | 이유 |
|---|---|
| Dogfood-driven backlog | 정책 확정 (2026-05-14) |
| 새 SKILL 추가 | 결정 1로 trace mining SKILL 안 만듦. 다른 결함도 기존 SKILL 변경으로 처리 |
| 새 agent 추가 | 4 agents 충분 |
| 4축 모델 재설계 | finding 없음 |
| `--autocode` HTML 자동 생성 | v0.3 결정 = scaffold-only 유지 |
| shader full WebGL2 template | stub 의도된 상태 |
| schema_version *read 검증* enforcement | v0.5 이월 (결정 4) |
| trace data를 *살리는* mining SKILL | 결정 1로 deprecate |
| dino-run | readonly 유지 |

---

## 6. Backward-compat 약속

| v0.3 자산 | v0.4 호환 |
|---|---|
| v0.3 milestones.json (write-only 필드 포함) | **읽힘** — SKILL이 read하지 않음 (deprecated). 사용자가 *수동 정리* 권유 |
| v0.3 pillars.json | 변경 없음 |
| v0.3 prototypes | 변경 없음 |
| v0.3 validation/self-test 결과 | 변경 없음 (top-level read만) |
| `kit_version` 부재 milestones.json | v0.3로 추정 후 정상 작동 |
| v0.3 사용자가 v0.4 SKILL 호출 | 정상 — kit_version 자동 부여 (gmk-init --migrate or 첫 write 시) |

**Breaking 영역**: Wave γ에서 write 경로가 사라지므로, v0.3 SKILL이 만든 write-only 필드는 *덮어쓰기는 안 됨*. 데이터 손실 0. *deprecate*만.

---

## 7. 검증 체크리스트

### Wave α 머지 전
- [ ] `gmk-validate` Sub-flags 섹션에 `--rebaseline`, `--accept-regression` 정의됨
- [ ] `gmk-port` Sub-flags 섹션에 `--force-rebuild = --stage 1 alias` 명시
- [ ] grep `--[a-z]+` SKILL 본문 → 정의 섹션 cross-check

### Wave β 머지 전
- [ ] prototype-rules Rule 13/14 추가
- [ ] 27 SKILL Preconditions 섹션이 Rule 13-14 reference

### Wave γ 머지 전
- [ ] write-only 9 필드 grep으로 write 코드 0건
- [ ] example.json 갱신 (deprecated 필드 제거)
- [ ] pillars-example.json 작성
- [ ] structure.md 완성
- [ ] migration policy 결정 (gmk-init --migrate 또는 첫 write 자동)

### Wave δ 머지 전
- [ ] prototype-rules Rule 15 추가
- [ ] 6 SKILL이 Rule 15 citation으로 routing 출력

### Wave ε 머지 전
- [ ] 11 작은 결함 모두 처리 or 명시적 v0.5 이월

### v0.4.0 릴리스 전
- [ ] CHANGELOG / CONCEPT / README 갱신
- [ ] plugin.json + marketplace.json v0.4.0
- [ ] HANDOFF.md 갱신
- [ ] audit 자료 (v0.4-audit-* 4개) 정리 또는 archive

---

*다음 단계: Wave α 시작.*
