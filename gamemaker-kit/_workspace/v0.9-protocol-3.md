# v0.9 Protocol 3 — Pre-release evaluator report

**Date**: 2026-05-18 (21:32 KST)
**Anchor**: HANDOFF.md v0.9 backlog (lines 52-172), Protocol 1 report `_workspace/v0.9-protocol-1.md`.
**Working-tree HEAD**: `bd819d3` (v0.8.0 post-release HANDOFF). All v0.9 work uncommitted.
**Pre-flight**: `bash scripts/check-plugin-meta.sh` exits 0 with all 7 checks PASS, 0 warnings. Plugin meta still at 0.8.0 (Step 8 not yet executed; expected).

---

## 1. Verdict

**ACCURATE.**

All 6 in-scope candidates (Tier 1 T1-A/B/C + Tier 2 T2-A/B/C) are closed with artifacts that match Protocol 1's reframed targets. Tier 3 T3-A/T3-B remain deferred as planned. No new defects introduced. Pre-flight is 7/7 PASS / 0 warnings against the working tree.

Two items sit just below the bar for "Major" and are flagged in §3 as Minor; neither blocks the v0.9.0 tag. Tier 1-C is split — the template tracking-row half is closed *in the working tree*, the CHANGELOG `### Process` section half is deferred to release Step 8 per the Resume Instructions, which is the correct ordering (CHANGELOG entry cannot be written before the release commit message).

This is the 4th consecutive release on track for ACCURATE (Protocol 1 expectedly UNDERSTATED, Protocol 3 ACCURATE; Protocol 4 will be checked post-release).

---

## 2. Per-candidate verification

| Candidate | Verdict | Evidence |
|---|---|---|
| T1-A (Rule 16 unreachable rows audit, 3 branches) | **PASS** | See §2.1 |
| T1-B (skill count "~28" → "29" 5-site sweep) | **PASS** | See §2.2 |
| T1-C (Process retrospective — template row done, CHANGELOG deferred) | **PARTIAL — by design** | See §2.3 |
| T2-A (gmk-port split decision artifact) | **PASS** | See §2.4 |
| T2-B (`_save_breaking: false` Path (b) drop) | **PASS** | See §2.5 |
| T2-C (v1.0 backward-compat inventory) | **PASS** | See §2.6 |

**5 PASS / 0 FAIL / 1 PARTIAL (T1-C, the CHANGELOG half is correctly deferred to Step 8 release commit).**

### 2.1 T1-A — Rule 16 kit_version unreachable rows audit

Annotation added at `skills/gmk-prototype-rules/SKILL.md:483-496`, a new `### Exercise paths (live vs safety valve, as of v0.9)` subsection following the existing `### Why this rule exists` section.

- **3-row safety-valve table (L487-492)** covers exactly the 3 branches Protocol 1 narrowed to: row 1 (kit_version absent), row 3 (kit_version > current), row 4 (kit_version unparseable). Row 2 is correctly labeled **Live**. **PASS** on scope.
- **Uniform policy** declared at L494: *"annotate as safety valve in this rule, do not add synthetic fixtures. If a real user-environment trip is ever reported, the row graduates from safety-valve to live and a regression fixture lands at that point."* Cites the v0.7 Rule 14 CYCLE precedent (`rule:388/398`) as the pattern being mirrored. **PASS** on policy uniformity (no mixing of synthetic-fixture and annotation styles across rows).
- **Audit-closure paragraph at L496** explicitly enumerates the 3 Protocol 1 REMOVE items (`early_fail`, `Math.random()`, Rule 14 CYCLE) and cites the Protocol 1 report `v0.9-protocol-1.md §2` as the rationale for excluding them. This means the contract table's 4-row authority is preserved (no contradicting claims added) AND the audit decision is auditable via the report cross-reference. **PASS**.
- **No conflict with the original 4-row contract table** (L458-463): the new annotation classifies each row by *exercise frequency*, not by behavior; it doesn't change what the kit *does* when each case fires, only flags that 3 of the 4 cases never currently fire in-repo. The two sections are orthogonal. **PASS**.

### 2.2 T1-B — Skill count sweep across 5 doc sites

All 5 cited sites bumped from "~28" to "29":

| Site | Before | After |
|---|---|---|
| `README.md:6` | *"~28 skills + 4 domain agents…"* | *"29 skills + 4 domain agents…"* (verified) |
| `README.md:43` | `## Skills (~28)` | `## Skills (29)` (verified) |
| `README.md:192` | `| Skills | ~28 | 72 | n/a |` | `| Skills | 29 | 72 | n/a |` (verified) |
| `CONCEPT.md:111` | `## 4. Skill matrix (v0.2 — ~28 skills, 4 domain agents)` | `## 4. Skill matrix (v0.2–0.9 — 29 skills, 4 domain agents)` (verified) |
| `CONCEPT.md:409` | `| Skills | ~28 | 72 |` | `| Skills | 29 | 72 |` (verified) |

- **CONCEPT.md:111 living-vs-snapshot policy decision applied**: the v0.2 anchor is generalized to "v0.2–0.9" — the doc is now declared a *living architectural document* rather than a v0.2 snapshot. This is one of the two valid policy choices Protocol 1 surfaced (`v0.9-protocol-1.md §2 M-3 VERIFY`). **PASS** with consistent decision (CONCEPT.md:409 doesn't carry the version anchor since it's a comparison table, so no parallel edit needed).
- **CHANGELOG.md `~28` survivals at L373 and L444 left untouched** per the explicit non-target ("Keep-a-Changelog 동결"). Verified by grep — these are the only two surviving instances of `~28` in the repo. **PASS**.
- **plugin.json:4 and marketplace.json:8/15 already say "29 skills"** per the HANDOFF non-targets — verified, no accidental edits. **PASS**.
- **No new defects in count claims** — pre-flight Check D passes; plugin meta description still claims "29 skills" against `skills/` count of 29 (verified by pre-flight output: `skills/ count: 29`). **PASS**.

### 2.3 T1-C — Process retrospective (PARTIAL by design)

Two-part candidate. Status:

- **Template tracking table row v0.9 filled** at `_workspace/handoff-backlog-template.md:102`:
  ```
  | v0.9 | 6 | 18 | 3 | 3.00 + 3 |
  ```
  Numbers match Protocol 1 report exactly (6 candidates → 18 corrections + 3 missing → ratio 3.00). **PASS**.
- **CHANGELOG `### Process — Protocol 1 retrospective` section: NOT YET WRITTEN.** This is **correct per the release ordering** in the task instructions and the HANDOFF Resume Step 8 — the CHANGELOG v0.9.0 entry is written *as part of* the release commit (Step 8), after Protocol 3 (this report). Writing it now would put the entry ahead of the commit it documents. The author's note in the task spec explicitly defers this half: *"The CHANGELOG `### Process — Protocol 1 retrospective` section is deferred to release Step 8 alongside the CHANGELOG v0.9.0 entry."* **PASS (deferral is intentional, not an oversight).**

Recommendation for Step 8 (informational, not a Protocol 3 gate): when authoring the CHANGELOG entry, use the template format declared at `handoff-backlog-template.md:80-82`:
```
**Protocol 1 corrections**: 18 items (7 ADD + 3 REMOVE + 6 VERIFY + 2 RECLASSIFY) + 3 missing candidates
**Ratio**: 3.00 (vs. v0.8 = 2.80, v0.7 = 1.75)
**F21 trend**: rising but explainable (Protocol 1 §5) — template-conformance dimension added, expected to fall ≤2.0 in v0.10 once authors write to the template shape natively.
```

### 2.4 T2-A — gmk-port split decision artifact

`_workspace/v0.9-gmk-port-split-decision.md` (87 lines) exists with:

- **Decision**: KEEP (no split in v0.9). Stated upfront L5. **PASS** on decision-candidate format (per Protocol 1 RECLASSIFY: "produce a written decision artifact").
- **Re-measured baseline at L13-19**: `wc -l` on 5 largest SKILL files. `gmk-prototype-rules` now reads **570 lines** (post-T1-A growth from 555 → 570 by the new §Exercise paths subsection — this is a real, expected drift caught by the artifact itself). HANDOFF v0.8 cited 519, Protocol 1 corrected to 555, T1-A grew it to 570 — the chain is documented at L16 as a comment. **PASS** on stale-baseline-correction.
- **Rationale at L42-56** covers 5 reasoned points: no compliance gate, operational cohesion of the 6 stages, Stage-6 too small to extract, no agent reports difficulty, peer-tier comparison with prototype-rules. **PASS** on substantive justification.
- **Re-open triggers table at L62-70** lists 5 conditions that would re-trigger the decision (>800 lines / new SKILL needs subset / per-stage hook in gmk-loop / user reports navigation difficulty / new doc-cap rule). This is the durability mechanism — keeps the decision *re-openable*, doesn't lock it forever. **PASS**.
- **Status at L82-86** explicitly marks the candidate CLOSED. **PASS**.

### 2.5 T2-B — `_save_breaking: false` Path (b) drop

- **Edit applied at `skills/gmk-merge-gate/SKILL.md:156`**: the warning text dropped the `_save_breaking: false` clause. Diff verified:
  ```
  - …Either run `/gmk-save-migrate <id>` or confirm the change is non-breaking
  -   by adding a `_save_breaking: false` note to the milestone.
  + …Either run `/gmk-save-migrate <id>` or confirm the change is non-breaking
  +   before merge.
  ```
  Single-line edit, surgical. **PASS**.
- **Grep verification**: `grep -rn '_save_breaking' skills/` returns **0 hits** (was 1 before). The kit no longer references the field anywhere in `skills/`. **PASS** on T2-B's stated success criterion.
- **Decision artifact at `_workspace/v0.9-save-breaking-decision.md`** (86 lines) documents Path (a)/Path (b) trade-off and 5-point rationale for choosing (b), including the v1.0 backward-compat-inventory consequence (Path (b) means no entry in the inventory, which is correct since the field no longer exists). **PASS**.
- **Symmetry argument with v0.7 Rule 14 CYCLE preservation reasoning (L54) is well-stated**: explains why CYCLE was kept as safety valve but `_save_breaking` is dropped — CYCLE was actively cited elsewhere, `_save_breaking` was a singleton with no consumer. **PASS** on consistency with prior decisions.
- **`_workspace/v0.9-save-breaking-decision.md` mentions `_save_breaking` 20+ times** — these are in the decision-doc body, intentional historical context. Not a Check B-style staleness issue; not in the kit's live doc-set (`_workspace/` is excluded from Check B's scan paths). **PASS** on no-collateral-damage.

### 2.6 T2-C — v1.0 backward-compat inventory

`_workspace/v1.0-backcompat-inventory.md` (134 lines) exists with the 6 categories the task spec promised:

| Category | Coverage |
|---|---|
| 1. Schema additive-only (Rule 16 row 2) | L19-33: 5 source citations + 3 break candidates |
| 2. Deprecated-field forgiveness ("no data loss") | L35-55: 5 source citations + 9 enumerated fields + 3 break candidates |
| 3. Bot hook API additivity (`_gmkApiVersion: 1`) | L57-70: 5 source citations + 2 break candidates |
| 4. Pillar shape free-text fallback (Rule 17 row 2) | L72-80: 1 source citation + 1 break candidate |
| 5. Endpoint terminology standardization | L82-92: 3 source citations + explicit "no break candidate" |
| 6. `human:` → `self-test:` row migration | L94-102: 1 source citation + 1 break candidate |

- **Source citations are concrete file:line references** (CHANGELOG, prototype-rules, structure.md, templates), verifying each guarantee has an in-repo anchor. **PASS** on traceability.
- **§"What this inventory is NOT" at L106-110** correctly disclaims this is *not* a v1.0 commitment, *not* exhaustive, *not* a CHANGELOG. Matches the "informational artifact" classification declared in HANDOFF T2-C. **PASS**.
- **§"Open question for v1.0 work-start" at L114-125** suggests a reasonable v1.0 break set (Cat 1 + Cat 4 + Cat 6) without prescribing it — preserves v1.0's freedom to decide. **PASS** on scope discipline (doesn't pre-empt v1.0 Protocol 1).
- **Re-verify clause at L133** flags that v0.10+ guarantees should add corresponding rows — keeps the doc evergreen rather than freezing at v0.9. **PASS**.

---

## 3. New defects scan

Pre-flight result: **7/7 PASS, 0 warnings** (Checks A through G). Detailed output verified — every check including the WARN-level Check G ("pillars in example: 3, valid kind declarations: 3 — OK") clears.

`grep -rn '_save_breaking' skills/` → **0 hits**. T2-B's success criterion met.

`grep -rnIwE 'endpoint' skills/ CONCEPT.md README.md _workspace/structure.md .claude-plugin/` → all hits in allowlist (verified visually: `gmk-dev-complete:59`, `gmk-self-test:107`, `CONCEPT.md:11`, `CONCEPT.md:40`, `marketplace.json:15`, `plugin.json:4`). No new "endpoint" tokens introduced. **PASS**.

`grep -n '~28\|28 skills' README.md CONCEPT.md CHANGELOG.md` → only 2 hits remain, both in `CHANGELOG.md:373/444` (historical v0.3/v0.2 entries, explicit non-targets). **PASS**.

Line counts (verified): `prototype-shader.html:299`, all templates under 300. `prototype-rules.md:570` (grew by 15 lines from T1-A's annotation section, expected). Check E still PASS. `gmk-port.md:684` (unchanged — KEEP decision, no edit). **PASS**.

Allowlist staleness: spot-checked `gmk-art-gen/SKILL.md:144` (the Rule 14 allowlist entry shifted by v0.8 T2-A). `skills/gmk-art-gen/SKILL.md:144` reads *"For the failed one: re-run `/gmk-art-gen m2-dragon-evo --assets dragon-rg-idle`"* — matches the justification text *"recovery instruction inside an ongoing flow"*. No drift. **PASS**.

**No Critical defects.**

**No Major defects.**

**Minor (do not block release)**:

1. **HANDOFF.md Resume Instructions Step 1-6 still phrased as future tense** despite being complete in the working tree. E.g., Step 4 reads *"`_workspace/v0.9-gmk-port-split-decision.md` 작성. split or keep. 결정 후 follow-up."* — the artifact already exists with the KEEP decision. A post-Protocol-3 polish pass could either (a) mark Steps 1-6 with ✅ like Step 0, or (b) move the completed steps to a "v0.9 work completed" subsection. The task instructions explicitly note this is acceptable as-is (*"Steps 0–6 should be marked or implied complete; Steps 7-10 pending"*), so this is informational. **Severity: Minor.** Will be naturally resolved by Step 10 (post-release HANDOFF rewrite).

2. **HANDOFF Step 8 says "5 사이트 version bump"** when describing the upcoming release work. There are actually 6 sites to bump on release: `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `_workspace/examples/pillars-example.json`, `_workspace/examples/milestones-example.json`, plus any `kit_version` strings in `gmk-init` schema examples (`gmk-init/SKILL.md:163` and likely `:196` per the v0.8 Protocol 3 §3 note). The "5 sites" phrasing was likely inherited from v0.8 wording; Check D will catch any miss at pre-flight. **Severity: Minor / expected** — Check D auto-catches the kit_version drift the moment plugin.json bumps. Mentioned only so it isn't missed during the bump pass.

3. **`_workspace/v0.9-save-breaking-decision.md:48` cites `gmk-merge-gate:159` for the "warning, not a gate failure" anchor.** The actual current line for that phrase is `gmk-merge-gate:158` (one line shift caused by T2-B's edit at L156 reducing the warning row by 1 line). Cosmetic — the decision-doc points to a phrase that's still in the file, just one line off. **Severity: Trivial.** Doc-internal cite, not exposed to the kit's check infrastructure.

No regressions in any live behavior. No accidental edits in unrelated files (verified by `git diff --stat`: only the 9 expected files modified, all consistent with v0.9 scope).

---

## 4. Template conformance check (HANDOFF v0.9 backlog)

Per `_workspace/handoff-backlog-template.md`, each candidate needs 4 fields: (1) file:line + grep query, (2) explicit non-targets, (3) classification, (4) defect-class link. Tier 3 candidates are deferred and per the task instructions *"conformance not required for skipped candidates"* — still scored for completeness but don't gate.

| Candidate | (1) file:line+grep | (2) non-targets | (3) classification | (4) defect-class | Score |
|---|---|---|---|---|---|
| T1-A Rule 16 unreachable rows | **present** (3 cites + grep query) | **present** (4 non-targets with reasons) | **present** (`audit-only`) | **present** (future-version defensive code…) | **4/4** |
| T1-B skill count sweep | **present** (5 cites + grep query) | **present** (2 non-target categories with reasons) | **present** (`sweep`) | **present** (doc-drift on count metric) | **4/4** |
| T1-C Process retrospective | **present** (2 cites) | **present** (2 non-target categories) | **present** (`process`) | **present** (F21 anchoring closure mechanism) | **4/4** |
| T2-A gmk-port split | **present** (1 cite + grep query) | **present** (2 non-target categories) | **present** (`decision-candidate`) | **present** (none — readability/maintenance) | **4/4** |
| T2-B `_save_breaking` decision | **present** (1 cite + grep query) | **present** (2 non-target categories) | **present** (path-conditional) | **present** (declared-but-half-applied field) | **4/4** |
| T2-C v1.0 backcompat inventory | **present** (4 cites + grep query) | **present** (2 non-target categories) | **present** (`single-fix`) | **present** (missing-release-readiness-artifact) | **4/4** |
| T3-A Check G FAIL promote (deferred) | partial (1 cite) | partial (1 non-target) | present (`structural-guard`) | present | 3/4 |
| T3-B Rule 14 line-cap fixture (deferred) | partial (1 cite + grep query) | partial (1 non-target) | present (2 options listed) | present | 3/4 |

**Aggregate for in-scope (6 candidates)**: **24/24 fields present = 100% template conformance**. Tier 3 deferred candidates are 6/8 (75%) but explicitly out of scope per task spec.

This is a dramatic jump from v0.8's 48% conformance (per v0.9 Protocol 1 §4). Cause: the v0.9 backlog was *rewritten* by Protocol 1 with full template fields in mind, then carried into the HANDOFF in that rewritten form. This is exactly the template-adoption effect Protocol 1 §5 predicted. **PASS**.

---

## 5. Release readiness

**GREEN-LIGHT v0.9.0 tag.**

All in-scope candidates verified present and correct. Pre-flight passes 0/0/0 (drift/warn/fail). T1-C's CHANGELOG half deferred to Step 8 is intentional and follows the documented release ordering. No new defects. 100% template conformance on the v0.9 backlog (vs. 48% in v0.8) — F21 closure machinery is working as designed.

**Recommended Step 8 sequence** (in order):

1. Bump `.claude-plugin/plugin.json` + `marketplace.json` to `0.9.0`. Update plugin.json description to reflect v0.9 deliverables (Rule 16 §Exercise paths annotation, _save_breaking drop, v1.0 backcompat inventory, 100% template conformance milestone).
2. Bump `_workspace/examples/pillars-example.json:3` and `_workspace/examples/milestones-example.json:3` to `"kit_version": "0.9.0"`. Check D will block release until this is done.
3. Also re-check `skills/gmk-init/SKILL.md` schema examples (L163 / ~L196 per v0.8 Protocol 3 §3 Minor #2) for `kit_version` strings to bump.
4. Update CHANGELOG.md with v0.9 entry. Include:
   - Standard release notes (Rule 16 exercise-paths annotation, skill count bump, _save_breaking drop, gmk-port KEEP decision artifact, v1.0 backcompat inventory).
   - `### Process — Protocol 1 retrospective` section per template format (deferred T1-C half).
5. Optional Minor #1 from §3 polish: mark HANDOFF Steps 1-6 as ✅ before release commit, OR defer to Step 10's post-release HANDOFF rewrite.
6. Commit, tag, push, GitHub release, then Protocol 4.

**This is the 4th consecutive release on track for ACCURATE** — Protocol 4 will be the final verification post-tag. Process is mature; the kit can ship.

---

## Appendix — Verification commands run

- `bash scripts/check-plugin-meta.sh` → exit 0, 7/7 PASS, 0 warnings.
- `git diff --stat` → 9 expected files modified, no surprises.
- `grep -rn '_save_breaking' skills/` → 0 hits.
- `grep -rnIwE 'endpoint' skills/ CONCEPT.md README.md _workspace/structure.md .claude-plugin/` → all 6 hits allowlisted, none new.
- `grep -n '~28\|28 skills\|~29\|29 skills' README.md CONCEPT.md` → 0 `~28` hits in README/CONCEPT, both "29 skills" sites verified.
- `grep -n '~28\|28 skills' CHANGELOG.md` → only L373 + L444 (historical, non-target).
- `wc -l skills/gmk-prototype-rules/SKILL.md skills/gmk-port/SKILL.md skills/gmk-validate/SKILL.md skills/gmk-self-test/SKILL.md skills/gmk-merge-gate/SKILL.md templates/prototype-*.html` → all under their respective caps; prototype-rules grew to 570 as expected.
- Allowlist spot-check `gmk-art-gen/SKILL.md:144` → text matches justification, no drift.
- File presence: `_workspace/v0.9-protocol-1.md`, `_workspace/v0.9-gmk-port-split-decision.md`, `_workspace/v0.9-save-breaking-decision.md`, `_workspace/v1.0-backcompat-inventory.md` — all 4 exist with substantive content (87/87/86/134 lines respectively).
