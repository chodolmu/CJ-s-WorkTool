---
name: execute-all
description: 현재 스프린트의 모든 태스크를 자동으로 연속 실행한다. "/execute:all", "전부 실행", "스프린트 돌려", "자동으로 쭉 진행해" 등을 말할 때 사용한다. 중간에 멈추지 않고 스프린트 끝까지 달린다.
---

# Execute All — 스프린트 자동 실행

현재 스프린트의 남은 태스크를 멈추지 않고 연속 실행한다.

## 사전 조건

- `/execute`와 동일

## 동작

1. 현재 스프린트의 미완료 태스크를 순서대로 실행
2. 각 태스크마다 `/execute`와 동일한 흐름 수행:
   - 컨텍스트 조립 → 모델 배치 → 실행 → handoff → 검증
3. 태스크 실패 시에도 멈추지 않고 다음 태스크로 진행
   - 단, 실패한 태스크에 의존하는 태스크는 건너뜀 (skipped)
4. 스프린트 내 모든 태스크 완료 후 결과 요약:

```
스프린트 1.1 "캔버스 세팅" 완료

  ✅ HTML 뼈대 만들기 (haiku, 32초)
  ✅ 게임 루프 구현 (sonnet, 2분 15초)
  ✅ 키보드 입력 처리 (sonnet, 1분 48초)

  소요: 4분 35초 | 비용: $0.15
  검증: build ✅ typecheck ✅

  → `/validate`로 스프린트 통합 검증을 실행하세요
  → `/execute:all`로 다음 스프린트를 계속할 수도 있어요
```

## 중단

사용자가 중간에 "멈춰", "중단", "잠깐" 등을 말하면 현재 태스크 완료 후 멈춘다.
이후 `/execute`로 한 개씩 또는 `/execute:all`로 다시 연속 실행할 수 있다.

## 주의사항

- 하나의 스프린트 단위로만 자동 실행한다. 마일스톤 전체를 한 번에 돌리지 않는다.
  이유: 스프린트 사이에 검증과 계획 갱신이 필요하기 때문.
- 스프린트가 끝나면 반드시 멈추고 검증을 안내한다.
